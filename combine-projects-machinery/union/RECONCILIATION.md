# Machinery reconciliation: ferrislicer × dwc-ng

Row format: id | ferrislicer (concise, project terms) | dwc-ng (concise,
project terms) | proposed universal form (lay language) | your feedback.

- `—` = the project has no version of this behavior.
- `⚔` prefix on the universal cell = genuine conflict; ferrislicer's demand
  won; the dwc-ng column shows what was overruled. Your feedback decides.
- "dropped nuance:" inside a universal cell = fine print deliberately
  simplified away; say so in feedback if it must survive.
- `→ cant-break-by-design § …` as the universal cell (group 9 only) = that
  demand is already stated in the named section of the mandatory skill, so the
  union points at it instead of restating it; the project columns still show
  where the demand reached us from.
- `, plus:` after such a pointer = the skill states the row's core demand, and
  what follows the `plus:` is the one thing the row adds and the union must
  still carry.
- `✗ not in union: ` prefix on the universal cell = the owner ruled this row
  project-specific; it stays here as the record and is not carried into the
  union.
- `✓ ruled: ` prefix on the universal cell = the owner settled this conflict at
  review; the universal cell is his ruling. The project columns stay as they
  were, so the overruled form is still on the record.
- Leave your verdict in the last column: blank/OK = approved as written;
  anything else = instruction for phase 3.

## Reading guide

579 rows in 15 groups: 1 Straight Talk 10, 2 Rule Governance 34, 3 Verification
& Evidence 50, 4 Agent Topology & Economy 25, 5 Worktree & Campaign Discipline
40, 6 Work Tracking 36, 7 Test Double & UAT Discipline 8 (excluded from the
union by owner ruling — see the group heading), 8 Tool Output Hygiene
32, 9 Design Invariants 185 (7 rows, 9.165–9.171, excluded from the union by
owner ruling — see the group heading), 10 Reference Sources 14 (9 rows,
10.6–10.14, excluded from the union by owner ruling — see the group heading),
11 Environment & Platform
11, 12 Standing Agents 47, 13 Register System 32, 14 Commit Gates 18, 15 Merge
Gates & Ratchets 37.

Two distinct disagreements across three ⚔ rows — a real conflict, where
ferrislicer's demand is the one proposed and the dwc-ng column shows what it
overrules. **2.6** and **14.3** are the SAME disagreement at two triggers (run
the governance check on every commit versus only on commits touching what it
checks; 2.6 states it for the register check, 14.3 for any commit gate), so
deciding one decides both. The other is **4.9** (agent classes). **4.20** (when
work needs its own working copy) was the fourth, and the owner ruled it at
review on 2026-09-01: it now reads `✓ ruled` rather than ⚔, and the ruling went
the other way — the general exception letting a single commit run on the shared
copy is gone, leaving only a named list of by-convention operations there.
Everything else either merged
cleanly or exists on one side only. Seven rows carry a `dropped nuance:` note,
where fine print was deliberately simplified away; say so in feedback if any of
it must survive.

Two groups are worth reading in halves rather than straight through. **Group 9**
has been reconciled against the mandatory `cant-break-by-design` skill, and the
skill is now the group's first seam: 9.1 states that the skill is mandatory, and
85 of the 185 rows point at it rather than restating it (`→ cant-break-by-design
§ …`) — 79 because the skill makes that demand in full, and six (9.13, 9.35,
9.85, 9.105, 9.106, 9.111) that keep a `, plus:` clause for the one thing they
add. A row counts as a pointer only when its universal cell BEGINS with
`→ cant-break-by-design`; rows that merely mention the skill are not pointers,
which is why grepping its name returns more rows (88 across the whole table,
86 of them group-9 universal cells) than the 85 counted here — 9.1 names it to
make it mandatory, and a handful of rows elsewhere name it in passing. Those
pointers cluster hard in the first half — 78 of the 88 rows up to
9.88 — which is the half about how strongly an invariant is held: the
enforcement ladder, the techniques, the catalogue of ways a claim outruns its
mechanism; the other seven pointers sit at 9.89 or later. The 99 rows
(185 rows less 85 pointers less the 9.1 mandate)
that extend the skill outright are what is left to read: nine stragglers in the
first half (9.11, 9.33, 9.34, 9.42, 9.51, 9.52, 9.72, 9.79, 9.84), then all of
9.89–9.185 bar seven rows — the standing invariants themselves and the method
for auditing them, which the skill does not state. So the old 9.89 split still
holds, and the second half is now where nearly all the remaining reading is.

**Group 5** reads in three parts: 5.1–5.23 are
ordinary working-copy lifecycle; 5.24–5.38 are one self-contained procedure —
the rebuild of a long-diverged parallel branch — which reads or skips as a unit;
and 5.39–5.40 are two loose rules belonging to neither half (what a working copy
is named for, and the go-ahead an effort needs before it starts). Group 12 is
long but needs no seam: 12.1–12.10 are the conventions every agent definition
shares, 12.11–12.45 are one agent at a time, and 12.46–12.47 are two
cross-cutting rules found after the rest — read those two, they apply to every
agent above them.

A temporal pass has also been run: within each project the dated rules were put
in date order and every later ruling was checked against the earlier one on the
same subject, which found two rulings that narrowed an earlier rule's scope
(both in group 9, on what may stand for absence) and no reversal whose earlier
form the table still carried — two rows were changed to state the later form and
name the earlier one, no row carries a superseded form, and no row was added.

One open item, deliberately not a row: dwc-ng's rule inbox carries an entry
captured 2026-09-01 ("the test loop shouldn't feed the agent unless there is a
failure") still marked pending. Adjudicating it is yours, not this table's, and
phase 3 must not lose it.

## 1. Straight Talk

*dwc-ng has no straight-talk section. Its honesty demands fire on verification
events (a diagnosis, a capability claim, a closed question) and are rowed in
group 3, not duplicated here.*

| id | ferrislicer | dwc-ng | proposed universal form | |
|---|---|---|---|---|
| 1.1 | "I don't know" said the moment it's true; never papered over with a plausible plan | — | Say "I don't know" the moment it is true. Not knowing is a reportable state, never hidden behind a confident-sounding plan. | |
| 1.2 | Every claim carries status: measured (command + output) or believed (labeled, plus what would verify it); "fixed"/"safe" with no evidence in the same message = violation | — | Every claim says whether it is measured or believed. Measured means the command and its output are shown; believed means it is labeled as a belief, with what would settle it. Calling something fixed, improved or safe with no evidence in the same message is a violation. | |
| 1.3 | Attribution only after isolation: "X caused Y" requires the isolating bisect or toggle; otherwise "not yet isolated" | — | Name a cause only once you have run the thing that isolates it. Until then, say the cause is not yet isolated. Row 3.14 adds the sharper version for a diagnosis. | |
| 1.4 | Deviations announced BEFORE they happen, never discovered later; told-one-thing-done-another is the target failure | — | If what you are about to do differs from what was agreed, stop and say so first. Being told about it afterwards is the failure this rule exists to kill. | |
| 1.5 | Failures and regressions reported immediately, at full strength, never softened, never buried under progress | — | Report a failure or a regression in the first message after you know, at full strength. Never soften it and never bury it inside a progress report. | |
| 1.6 | The uncomfortable sentence comes first: if the honest summary is "this didn't work," that is the opening line | — | Lead with the uncomfortable sentence. If the honest summary is that it did not work, that is the first line, not a caveat at the end. | |
| 1.7 | HARD RULE; outranks looking competent | — | When honesty costs you the appearance of competence, honesty wins. This rule outranks looking good. | |
| 1.8 | Maximally concise, no meaning lost: lead with the answer, cut preamble, recaps, hedging | — | Be as brief as you can without losing meaning. Lead with the answer, and cut preamble, recaps of what was just said, and hedging. | |
| 1.9 | Plain analogy over technical vocabulary; no computational-geometry terms or other esoterica in user-facing text | — | Explain with a plain analogy instead of specialist vocabulary. Jargon a reader has to decode does not belong in what they read. | |
| 1.10 | Scope: chat only. Commits, issues and code comments keep technical vocabulary — precision matters there | — | This applies to conversation only. Written artifacts such as commits, tickets and code comments keep their precise technical vocabulary, because precision matters there. | |

## 2. Rule Governance

*The rule-intake procedure's commit hygiene demands (explicit pathspecs, ticket
marker in the message) live with the commit rules, not here; only "file the
three files together" is rowed below.*

| id | ferrislicer | dwc-ng | proposed universal form |                                                                                                               |
|---|---|---|---|---------------------------------------------------------------------------------------------------------------|
| 2.1 | Capture hook fires only on the exact `RULE:` prefix (case-insensitive token); no language matching, no regex phrase-guessing, no LLM classification | Same hook, same ruling, ported unchanged: the explicit mark is the sole trigger | A dictated standing rule is marked by an agreed prefix on the prompt, and that mark is the only trigger for capture. Nothing guesses at phrasing and nothing classifies intent. |                                                                                                               |
| 2.2 | Marked prompts appended verbatim before the model responds; unmarked prompts never stored; capture happens even if the session ignores it or dies mid-turn | Identical mechanism | A marked prompt is written to the inbox verbatim, before the assistant replies. Unmarked prompts are never stored, and capture still happens if the session ignores the rule or dies part-way through. |                                                                                                               |
| 2.3 | Inbox entry: heading of UTC timestamp plus session id, the prompt verbatim, then `Disposition: PENDING` | Identical shape, written by the same hook | Each captured rule becomes one inbox entry: a heading with the time and the session it came from, the prompt word for word, and a disposition line that starts as pending. |                                                                                                               |
| 2.4 | Capture prints into the turn: file it now via the intake procedure, and names the consequence — "commits are blocked until then" | Same print, consequence stated as its own gate: the fast register check will fail until then | The moment a rule is captured, the session is told to file it now and told exactly what will fail until it does. |                                                                                                               |
| 2.5 | Any `Disposition: PENDING` entry fails `register_check.py` | Same check, `--fast` mode included | An undispositioned inbox entry fails the register check. Filing is not optional bookkeeping; the check stays red until it is done. |                                                                                                               |
| 2.6 | Pre-commit runs the governance checks on EVERY commit in a clone with `core.hooksPath` set, and CI runs them on push | Pre-commit runs only when the commit touches one of five governance paths; activated per clone by `pnpm hooks:install`; no CI at all | ⚔ Run the governance check on every commit, and again in continuous integration so a machine that never installed the hook is still caught. dropped nuance: the narrower path-filtered form was a deliberate choice, on the argument that a gate firing on every commit is a gate people switch off. | The gate on dwc-ng was taking over an hour. we need to come up with more focused gates per project in general |
| 2.7 | Inbox header states exactly where the gate runs and where it does not: hooks-path clone yes, CI yes, a bare commit with no hooks path no | Skill states the hook does gate, activated per clone, and that what is missing is CI plus any clone that never installed the hook | State exactly where the gate does and does not run, in the same breath as claiming it exists. An opt-in hook and a missing continuous check are both holes, and naming them is part of the claim. |                                                                                                               |
| 2.8 | Editing a rule-bearing doc while the register has no pending change prints an advisory reminder to update the register in the same commit; never blocks | Same hook, project's own rule-bearing paths; here it doubles as a "did you mean to hand-edit a generated file" signal | Editing a rule-bearing document while the register sits untouched prints a reminder to update the register in the same commit. It is advisory and never blocks. |                                                                                                               |
| 2.9 | A change that adds, changes or supersedes a rule updates the register in the SAME commit as the durable-home edit | Same demand, stated in the register's own maintenance contract | A change that adds, changes or supersedes a standing rule updates the register in the same commit as the rule's own home. |                                                                                                               |
| 2.10 | The register CITES, it never ORIGINATES: write the rule where it lives first, then add the citing row | Same wording in its contract and its intake skill | The register points at where each rule actually lives. Write the rule in its home first, then add the row that cites it. A rule that exists only in the register has no home. |                                                                                                               |
| 2.11 | Durable home by kind: process rules to CLAUDE.md, engine/design rules to the owning spec, enforcement-strength claims to `docs/INVARIANTS.md` | Same split, but enforcement-strength claims are declared beside the mechanism in source and regenerated; the skill never hand-edits that file | Send each rule to the home its kind belongs in: working-agreement rules to the main agreement document, design rules to the specification that owns that subsystem, and claims about how strongly something is enforced to the enforcement ledger — declared beside the mechanism itself, never hand-typed into a generated file. |                                                                                                               |
| 2.12 | Contract: citations name a SECTION, never a line, for CLAUDE.md — a line cites a POSITION and any insert above silently retargets the row; checker-enforced, though its own intake skill still says `file:line` | Same contract, same CLAUDE.md-only scope, same reason and same checker; its intake skill extends the preference to every file while conceding the check covers CLAUDE.md | Cite the main agreement document by section, never by line number: a line number names a position, and any edit above it silently moves what the row points at. The same reasoning argues for section citations everywhere, and one project's filing procedure already asks for that. |                                                                                                               |
| 2.13 | Candidate search is token-cheap: read the contents list and plausible group summaries, grep for key nouns, shortlist two or three, present with a one-line reason each | Identical step | Find where a new rule belongs cheaply: read the table of contents and the summaries of likely groups, search for the rule's key words, then put a shortlist of two or three in front of the owner with one line of reasoning each. |                                                                                                               |
| 2.14 | For each candidate group, decide whether the new rule AGREES, REFINES, or CONTRADICTS its standing | Identical step | For each candidate group, say whether the new rule agrees with it, sharpens it, or contradicts it. |                                                                                                               |
| 2.15 | A contradiction IS a wobble: update the group's "wobble / where it ended" with the new verdict and flip the status circle | Identical, with the circle flipped green unless marked tentative | A contradiction is recorded as an unsettled question that has now been decided: write the verdict into the group's own account of the disagreement and update its status marker. |                                                                                                               |
| 2.16 | Supersession is stamped both directions in the same commit: the losing doc gets a stamp pointing at the register group, the new row a `Supersedes:` line; old bodies never rewritten | Same, with the exact stamp text specified and both directions machine-checked | When one rule supersedes another, stamp both directions in the same commit: the retired document points at the group that replaced it, and the new row names what it supersedes. The old text is never rewritten in place. |                                                                                                               |
| 2.17 | Red flag: "this obviously supersedes X" — supersession is Gabe's call when the losing rule was ever his; propose, don't presume | Identical red flag | Supersession is the owner's call whenever the losing rule was theirs. Propose it; never decide it. |                                                                                                               |
| 2.18 | Placement ladder: fits a group, append a dated row; no group but related solo rules exist, promote them into a new group with a summary; no siblings, a row in the matching solo area | Same ladder, creating the solo section if this is the first solo rule | Place a new rule by ladder: if a group fits, append a dated row; if no group fits but related loose rules exist, promote them together into a new group with a summary; otherwise file it as a loose rule under its area. |                                                                                                               |
| 2.19 | Only an adjudication — the owner's ruling or a landed change that settles it — flips a group's status circle toward settled | Identical rule | Only a real decision moves a group's status marker to settled: the owner's ruling, or a change that lands and settles the question. Nothing else upgrades it. |                                                                                                               |
| 2.20 | Each group summary states what binds its rules and, where the project wobbled, what the wobble argued about and where it ended | Identical contract | Every group says in plain words what its rules have in common and, where the team changed its mind, what the disagreement was about and how it ended. |                                                                                                               |
| 2.21 | Groups whose rules are enforced in code list their enforcement-ledger anchors and keep them resolving | Groups state their enforcement honestly, including "none mechanical — held by review and by the post-mortem trigger" | Every group states how strongly its rules are actually enforced, naming the mechanism where one exists and saying plainly that nothing enforces it where none does. |                                                                                                               |
| 2.22 | Disposition replaces PENDING with "filed — group (home)" or "not a rule — reason"; the home, the register and the inbox are committed together | Identical, same three files in one commit | Close the loop on a captured rule by replacing pending with either where it was filed or why it is not a rule, and commit the rule's home, the register and the inbox together. |                                                                                                               |
| 2.23 | Fast mode covers every check but one; CI runs the full check on push | Run the FULL check by hand before calling a filing done — the group and status scan is the fast mode's blind spot | Run the complete register check before calling a filing done. The cheap commit-time subset is not the whole check, and what it skips is exactly what a bad filing looks like. |                                                                                                               |
| 2.24 | Red flag: "the inbox entry can wait" — it can't; it blocks every commit by design | Red flag: file it or dismiss it with a reason; don't leave it pending across a commit just because nothing mechanically stops you | Never carry an undispositioned rule across a commit. File it or dismiss it with a reason, whether or not a machine is stopping you. |                                                                                                               |
| 2.25 | Inbox entries are append-only history; never delete them (duplicate hand-captures are kept, annotated) | — | The inbox is append-only history. Entries are never deleted, and a duplicate is kept with a note rather than tidied away. |                                                                                                               |
| 2.26 | Rules ruled in conversation without the marker are hand-captured with a note saying why the hook did not fire | Same practice, same annotation style, including rulings given under a different prefix | A rule ruled in conversation without the marker is still written into the inbox by hand, with a note saying why the automatic capture did not fire. |                                                                                                               |
| 2.27 | A post-mortem's candidate rule goes through intake as a proposal, never self-filed: agents do not adjudicate | Trust property stated first: the procedure never self-files, and only the owner adjudicates a rule | An assistant never files or adjudicates a rule itself. Every candidate rule is a proposal to the owner. |                                                                                                               |
| 2.28 | Trust property: the intake procedure is NOT load-bearing — capture and enforcement are hooks and scripts, so no rule can be silently lost if it never runs | Same property: if the procedure never runs, the entry just sits pending and the check keeps reporting it | The filing procedure is not what keeps rules safe. Capture and the check are mechanical, so a rule cannot be silently lost even if nobody ever runs the procedure — it just stays visibly unfiled. |                                                                                                               |
| 2.29 | Any VIOLATED verdict in a change's confirmation pass mechanically launches a post-mortem agent; "I already know why" is not an exemption | Cited as the enforcement for its authorship rules: the post-mortem trigger that fires on a ledger mismatch | When a stated expectation turns out false, a post-mortem is launched automatically. The trigger is mechanical, and already knowing why is not an exemption — the explanation that feels obvious is the one that never gets written down. |                                                                                                               |
| 2.30 | Post-mortem runs on the mid-tier synthesis model, dispatched alone, its result verified before anything else runs | — | The post-mortem runs as its own dispatched job on a mid-tier model, alone, and its result is verified before anything else starts. |                                                                                                               |
| 2.31 | Output is rule-shaped, not narrative: what was believed, what was true, WHERE the belief entered, which standing rule would have caught it; "we should be careful" produced nothing | — | A post-mortem answers four things: what was believed, what was actually true, where the belief entered, and which standing rule would have caught it. A conclusion of "we should be more careful" has produced nothing. |                                                                                                               |
| 2.32 | Session start echoes the governance banner: how to dictate a rule, where it is captured, what blocks until it is filed, and where the register lives | Same banner, stating its own gate and the absence of CI | Every session opens with the governance protocol: how to dictate a standing rule, where the capture lands, what stays blocked until it is filed, and where the register lives. |                                                                                                               |
| 2.33 | Register seeded from a dated extraction snapshot of 432 rules, itself not maintained; hand-maintained since | No provenance snapshot exists and one must not be invented; if wanted, generate it from version-control history rather than writing it by hand | Say where the register's rows came from. If no provenance record exists, do not write one by hand — generate it from version-control history or do without it. |                                                                                                               |
| 2.34 | A `path:line` citation landing on a BLANK line is BLOCKING everywhere: shipped advisory at 13 occurrences, those were repaired to section form, and it was promoted in the commit that emptied it | Same check, ported live and blocking; currently a no-op because every row already uses the section form, kept for future line citations into docs with no section gate of their own | Where a citation still names a line, that line must point at real content. A citation landing on a blank line is not a weak citation, it is no citation, and it blocks. |                                                                                                               |

## 3. Verification & Evidence

*The gate-side instances of these rules — a gate printing its own denominator,
a merge gate not trusting parent order — fire when a gate runs and belong with
the gate rules, not here. Proving a change against a stand-in for the real
system is likewise its own group. 3.38–3.50 were added by the coverage sweep:
3.38 is the capture that must render what a real user sees, and 3.39–3.50 are
the performance-measurement method, which is one long answer to the same
question this group asks everywhere else — what did the measurement actually
see, and would it have been able to come out differently.*

| id | ferrislicer | dwc-ng | proposed universal form | |
|---|---|---|---|---|
| 3.1 | A change ships with its ASSUMPTIONS COMPILED — the beliefs that make the choice and the design legitimate | — | Every change writes down the beliefs that make its design and its choices legitimate. | |
| 3.2 | Expected BLAST RADIUS names both halves: what SHOULD change and what SHOULD NOT; the "should not" half carries the information | — | Every change predicts what it will affect and, just as explicitly, what it must leave alone. The second half is the one that can be proved wrong, so it is the half that carries the information. | |
| 3.3 | Written BEFORE the coding: assumptions compiled after the fact describe what happened, they are not a prediction that could have failed | — | Write the predictions before doing the work. Written afterwards they only describe what happened; they were never able to fail. | |
| 3.4 | Confirmed AFTER: each assumption and each expected truth named and reported HELD or DID NOT HOLD — never a blanket "tests pass" | — | After the work, confirm each prediction one by one and say for each whether it held. A blanket "everything passed" is not a confirmation. | |
| 3.5 | An unconfirmed expectation is an UNFINISHED change — not a caveat, not a follow-up ticket | — | A prediction left unconfirmed means the work is not finished. It is not a caveat and not a follow-up item. | |
| 3.6 | A violated expectation is a REPORTABLE RESULT at full strength; whether the change was wrong or the assumption was is an explicit finding | — | A prediction that fails is a result to report at full strength, not a mess to tidy. Say explicitly which was wrong: the work, or the belief behind it. | |
| 3.7 | The change DECLARES its output bar in advance — byte-identity for value-neutral work, structural parity plus invariants for deliberate algorithmic work; mislabelling a behaviour change as a refactor dodges the bar | — | A change declares in advance which standard it is claiming: identical output, because it was meant to change nothing, or structural checks, because it deliberately changes behaviour. Calling a behaviour change a cleanup is how that standard gets dodged. dropped nuance: which standard applies to which kind of work is a separate standing decision, not the author's to pick. | |
| 3.8 | A claim of byte-identical output must be measured by building and diffing, never assumed | — | A claim that output is unchanged is measured by producing the output and comparing it. It is never assumed. | |
| 3.9 | State what the measurement COULD see in the same breath as the claim; scope that differs from the claim is evidence for a DIFFERENT question that reads exactly like evidence for yours | Named as one generator: a check that can only speak to the instance in front of it was treated as proof of the class, premise or diagnosis it was attached to | Say what your check could actually see, in the same breath as the claim it supports. A check whose reach is narrower than the claim is evidence for a different question, and it looks exactly like evidence for yours. | |
| 3.10 | Ask WHY the result looks like that, not WHETHER it does — every failed case was green, plausible, and answering a question nobody asked | — | Ask why the result looks the way it does, not merely whether it looks right. Green and plausible is what a wrong answer to an unasked question looks like. | |
| 3.11 | A subagent's confidence is not scope: an enumeration returned 21 rows at CERTAIN where the file held 22 — verify the COUNT independently | — | Someone else's confidence is not evidence of coverage. Verify counts yourself, because a complete-looking list is not proof of completeness. | |
| 3.12 | A clean check does not shorten the list: required checks enumerated BEFORE the first one runs; a pass on one never cancels another. A positive result is the most dangerous place to stop | — | List the checks a change requires before running the first one, and let no pass cancel another. A good result is the most dangerous place to stop, because stopping then feels earned. | |
| 3.13 | — | A finding or ruling using a class noun ("class", "shape", "pattern") must search the changed layer and fix or defer every hit BY NAME before the task closes | If you call a defect a class, a shape, or a pattern, you owe every instance of it: search the area you changed and fix or defer each one by name before closing. Fixing the one you were shown does not discharge a claim about a class. | |
| 3.14 | — | A diagnosis is not "confirmed" until the call chain is cited hop by hop, file and line, from the entry point that runs on the ACTUAL observed input to the indicted line | A diagnosis is confirmed only when you can trace the path, step by step, from the real observed input to the line you are blaming. Reading the end of the code correctly does not show that execution ever reaches it. | |
| 3.15 | — | A fix for a falsified premise states what the broken premise assumed about its INPUTS and checks whether the replacement inherits it one layer down | When the task exists because an earlier belief was proved false, say what that belief assumed about its inputs, and check whether the replacement quietly assumes the same thing one layer down. | |
| 3.16 | — | Closing an open question requires naming the variable whose value would flip the answer, as something a later reader could check and find FALSE; only prose means it is a guess with a due date | A question is closed only when you can name what would have to be true, in a form a later reader can check and find false. If all you can write is prose, the question is still open. | |
| 3.17 | — | Before any action leaving the working tree, verify the target from the environment, not from a document: the tracker's remote, the configured host, the branch | Before doing anything that reaches outside your own working copy, get the target from the environment itself, not from a document. A document copied from another project carries that project's addresses. | |
| 3.18 | — | Before claiming a hook, test, gate, flag, script or harness exists or would catch something, run the check that could FALSIFY it; this repo's own prose is the most dangerous source, because it reads authoritative and goes stale silently | Before claiming that some piece of tooling exists, blocks something, or would catch a given fault, run the check that could prove you wrong. The project's own documentation is the most dangerous source, because it reads as authoritative and goes stale silently. | |
| 3.19 | — | These rules fire on an ARTEFACT: when you write the word, the check is due | The trigger is the word you just wrote. Writing "class", "confirmed", "closed", or naming what the tooling can do is what makes the matching check due. | |
| 3.20 | TDD: a failing test written first and verified red, then the minimal implementation to go green | — | Write the failing test first and watch it fail, then write the smallest code that makes it pass. | |
| 3.21 | Every bug fix, feature, function, class and method ships with a unit test, a fixture generator, and coverage in every related integration test | — | Every fix and every new unit of code ships with its own test, something that generates its test data, and a case added to each broader test that covers it. | |
| 3.22 | Tests move with the code they cover when it is relocated | — | When code moves, its tests move with it. | |
| 3.23 | Suites run without fail-fast, and test output is never head-truncated | — | Run the whole suite rather than stopping at the first failure, and never truncate its output. | |
| 3.24 | A regression test on a multi-value setting asserts on the DEFAULT, not only a non-default one, or it cannot fail the way real users fail | — | A regression test covers the default setting, not only an unusual one, or it cannot fail the way real users fail. | |
| 3.25 | A suite that cannot express a given failure mode is not coverage for it, even while every test passes | — | If a suite has no way to express a given failure, it is not coverage for that failure, no matter how green it is. | |
| 3.26 | An ABSENCE asserted against a capture is only coverage if the capture was guaranteed able to see the event; absence assertions state what would have proved the capture alive | — | Asserting that something did not happen only counts if the observer was capable of seeing it happen. Say what would have proved the observer was alive. | |
| 3.27 | The whole workspace suite passes at the end of every committable step | — | The full suite passes at the end of every step you would commit. | |
| 3.28 | The view layer is never unit-tested (a test must never open a window); it is verified by build, lint and a manual smoke run | — | Where a unit test cannot honestly reach — a layer that would have to open a real window — say so and verify by build, lint and a hands-on run instead of pretending coverage. | |
| 3.29 | Parity verdicts come from diffing the mismatch set against a known baseline, never from the harness's exit code | — | Judge a comparison run by diffing its failure set against a known baseline, never by its exit code. | |
| 3.30 | Unexpected toolpath drift means stop and reconcile; a golden update is never used to paper over it | — | When a baseline moves unexpectedly, stop and work out why. Updating the baseline to make the check pass is never the fix. | |
| 3.31 | A rebaked golden proves self-consistency, never parity; every regeneration is diff-reviewed by a human | — | A regenerated baseline proves only that the tool agrees with itself. A person reviews every regeneration line by line. | |
| 3.32 | A SKIPPED case is unproven, not passed, and every skipped case is listed in the report | — | A skipped case is unproven, not passed, and every one of them is named in the report. | |
| 3.33 | Never edit source code just to make a comparison row pass; describe the fix and stop | — | Never change the code just to make a check go green. Describe the fix and stop. | |
| 3.34 | Never tighten a comparison tolerance to manufacture a finding | — | Never adjust a comparison's tolerance in order to produce the finding you wanted. | |
| 3.35 | A diff without whitespace suppression is not a diff of the CODE: a line rewritten with different whitespace appears on both sides, so a count over added lines counts an unchanged line as new. The whitespace-suppressed summary is the discriminator — deletions collapsing to zero is the tell | — | A comparison that does not suppress whitespace is not a comparison of the code: a line merely re-indented shows up as both a removal and an addition, so anything counting added lines counts unchanged code as new. Run it again ignoring whitespace: the deletions collapsing to nothing is what tells you. | |
| 3.36 | A test guarding an algorithm's COMPLEXITY asserts the algorithm's own work census — operations performed against the entitlement the input census buys — never a wall-clock ceiling: on a shared runner a stopwatch measures the runner and cannot tell a loaded machine from a quadratic loop. A generous wall clock may remain as a hang backstop, never as the verdict, and the census is asserted with a positive control | — | A test that guards how an algorithm scales asserts the work it actually did, measured against what its input entitles it to — never a time limit. On a shared machine a stopwatch measures the machine, and it cannot tell a busy one from an algorithm that got slower. A generous time limit may stay as a backstop against hanging, never as the verdict, and the work count ships with a positive control, because something that never ran satisfies any upper bound for free. | |
| 3.37 | A refactor task FIRST moves existing code into its new module with no behaviour change and the tests still green, and only THEN generalizes the behaviour | — | A refactor lands in two steps, never one: move the code with no change in behaviour and the tests still passing, then change the behaviour. Done together, nothing can tell you which half broke it. | |
| 3.38 | A test capture of the log reproduces the line an OPERATOR sees, BYTE FOR BYTE: a capture that renders a field its own way is evidence about itself, not about the log. The capture kept its own five format calls beside the real renderer; the one that disagreed made a quoted field invisible to every test, caught only by eyeballing a real run. The capture now composes its message from the same fields and reaches ONE renderer the same three ways the real one does, pinned by running both in the same session and diffing the lines | — | A test that captures output for checking reproduces the line a real user sees, byte for byte. A capture that renders a field its own way is not evidence about the output, it is evidence about itself — so the capture and the real renderer are one renderer, reached the same way, and the equivalence is pinned by running both over the same input and comparing the lines (9.89, 3.26). | |
| 3.39 | A per-stage timer wraps only the actual work, NEVER the branch that decides whether to run it, so a zero reading means "did not run" rather than "too fast to measure" (stated twice, independently) | — | A timer wraps the work and nothing else — never the test that decides whether the work runs. Otherwise a zero reading means both "it did not happen" and "it was too fast to see", and nothing distinguishes them. | |
| 3.40 | NEVER compare two performance builds run sequentially: build both binaries first, then INTERLEAVE the runs (A, B, A, B) and report every round, because sequential runs measure machine drift as much as the change | — | Never compare two performance builds by running one after the other. Build both first, then alternate between them and report every round: run sequentially, you are measuring how the machine drifted as much as what you changed. | |
| 3.41 | Performance measurement always runs from your own checkout against a SEPARATE working copy; there is no option and no default for measuring the tree you are actively editing | — | A performance measurement runs against a separate working copy, never the tree you are editing. There is deliberately no option for the other thing, because editing under the measurement is how a number becomes unattributable. | |
| 3.42 | A correctness question NEVER rides along inside a performance or profiling commit: it needs its own commit, its own fixture, and its own golden/oracle verification | — | A correctness question never rides along inside a performance commit. It gets its own change, its own test data, and its own comparison against the known-good result. | |
| 3.43 | Always record and STATE which configuration a performance measurement was taken under, since the dominant cost — and therefore the conclusion — changes with the configuration | — | Always record and state the configuration a performance measurement was taken under. Which part dominates the cost changes with it, so the conclusion does too, and a number without its configuration cannot be compared with anything. | |
| 3.44 | Measure on a QUIET machine: background load, such as a concurrent build, produces confidently wrong conclusions | — | Measure on a quiet machine. Background load — another build, most obviously — does not produce noisy conclusions, it produces confident and wrong ones. | |
| 3.45 | Instrument a performance fix in the SAME build as the measurement that tests it, so one run shows both whether it worked and why | — | Put the instrumentation for a performance fix in the same build as the measurement that tests it, so a single run shows both whether it worked and why. | |
| 3.46 | Instruments — census, capture, overlays — live in the SAME build as the changes they verify | — | Any instrument built to verify a change ships in the same build as the change. Verifying one build with an instrument compiled into another is comparing two things, not measuring one. | |
| 3.47 | Before reapplying a performance technique at a new call site, FIRST measure the fraction of inputs that are skippable there; that fraction, not the shape of the code, decides whether the technique helps | — | Before reusing a technique that works by skipping work, measure what fraction of the inputs at the new site can actually be skipped. That fraction decides whether it helps, not the fact that the code looks the same shape. | |
| 3.48 | Enabling a previously-dead feature path must not silently MULTIPLY the cost of the pipeline's most expensive predicate: duplicated derivation is collapsed to one before the feature goes live | — | Turning on a path that has never run must not multiply the cost of the most expensive thing the system already does. Where doing so would derive the same expensive fact twice, collapse it to one before the path goes live. | |
| 3.49 | Measuring one variable while another DOMINATES the outcome measures nothing: isolate variables one at a time before trusting a measured effect | — | Measuring one variable while a second one dominates the outcome measures nothing at all. Isolate them one at a time before you trust an effect you think you have measured. | |
| 3.50 | Measure FIRST: instrument and observe, rather than reasoning about where a boundary or a defect ought to be | — | Measure first. Instrument the thing and look, instead of reasoning about where the boundary or the defect ought to be — the reasoning is what you are trying to check. | |

## 4. Agent Topology & Economy

*The worktree lifecycle rules — one per campaign, merge when green, delete
branch and worktree — belong with the worktree group; only the dispatch-time
demands are rowed here. Read-only constraints on reviewing agents are a
separate group.*

| id | ferrislicer | dwc-ng | proposed universal form |    |
|---|---|---|---|----|
| 4.1 | Efforts START in agents: the main loop's own work is design, adjudication, verdicts and verifying a returned result; everything else is dispatched | The main agent does no work: it holds conversation, adjudication and relay, and delegates everything else | Work is dispatched to a subordinate agent by default. The main conversation keeps only design, adjudication, verdicts, relaying, and checking what comes back. |    |
| 4.2 | NO size threshold decides this; a proposed threshold was raised and rejected | The line is intent, not file count: reading a file to ANSWER a question is conversation, reading it to CHANGE it is work | Size never decides whether to dispatch. The test is intent: reading something to answer a question is conversation, reading it in order to change it is work, and work is dispatched. |    |
| 4.3 | The cost conserved is main-loop CONTEXT, not model spend — tool output an agent absorbs and returns as a summary | Identical wording and identical reason | What this conserves is the main conversation's own attention, not money. A dispatched agent absorbs the raw tool output and hands back a summary. |    |
| 4.4 | Loading a skill's full body into main-loop context to do the work yourself IS doing the work; the skill invocation goes inside the dispatched agent's prompt | — | Pulling a procedure's full text into the main conversation to follow it yourself is doing the work. Put the procedure's invocation inside the dispatched agent's instructions instead. |    |
| 4.5 | Cheapest capable model per subagent task; quality held by verification gates, never by model tier | — | Use the cheapest model that can do each dispatched task. Quality is held by the checks on the result, never by paying for a bigger model. |    |
| 4.6 | A cheap agent whose spot-check fails gets its batch redone one tier up — recorded, never silent | — | When a cheap agent's work fails its spot-check, redo that batch one tier up and record that you did. Never re-run it silently. |    |
| 4.7 | Campaign plans state the model per task type | — | A plan says which model each kind of task runs on. |    |
| 4.8 | Exactly ONE subagent at a time PER TASK CLASS; across classes, parallel IS allowed with no asking | One agent in flight per class; concurrent across classes | One agent at a time within a kind of task. Different kinds may run at the same time without asking. |    |
| 4.9 | Classes are open-ended: effort, review, rule-intake, others as one gets named | Exactly four classes — effort, review, test, rule-intake — and a new class is named by Gabe, not inferred by an agent that wants a second slot | ⚔ The standing kinds of task are doing the work, reviewing it, and filing rules, and the set stays open: name a new kind when the work genuinely calls for one, and only the owner may name it — an agent never invents a kind in order to claim a second slot. dropped nuance: the overruled form fixed the set at exactly four, which also caps how many agents can be alive at once, and it names exercising the product as a kind in its own right. | OK |
| 4.10 | Dispatch one per class, wait, verify its RESULT, then the next in that class; "independent tasks within a class" is not a licence to parallelise | One agent in flight per class | Dispatch one, wait for it, check its result, and only then dispatch the next of that kind. Tasks being independent is not permission to run them together. |    |
| 4.11 | Every dispatch prompt forbids the agent spawning its own subagents or using the agent and workflow tools; an agent that fans out is a fan-out | — | Every dispatch tells the agent it may not spawn agents of its own. An agent that fans out is a fan-out regardless of what kind of task it was. |    |
| 4.12 | `ListAgents`, not TaskList, shows which agents are actually live across every class; any doubt, check before dispatching | — | Before dispatching, check what is actually running, using the view that shows live agents rather than a list of tasks. |    |
| 4.13 | Workflows fan out by construction: never started without the user explicitly asking first, never on the agent's own initiative, even proposed as a question | — | Anything that fans out by construction runs only when the user explicitly asks for it, never on the assistant's initiative and never floated as a suggestion. |    |
| 4.14 | A long task of many stand-alone units is dispatched in BATCHES: never one agent per unit, never every unit in one agent; both extremes are the failure | — | A long job made of many self-contained pieces is dispatched in batches. One agent per piece pays the setup cost over and over; all of it in one agent buries the late pieces and loses everything on a single failure. |    |
| 4.15 | A GRANULARITY rule only: batches still dispatch ONE AT A TIME within a class, each verified before the next | — | Batching changes how much goes into one dispatch, never how many run at once. Batches still go one at a time, each checked before the next. |    |
| 4.16 | The batch-sharing test is "can run on their own": no ordering constraint, no shared state; a unit fed by another's output is not independent | — | Pieces may share a batch only if they can run on their own: no ordering between them and no shared state. A piece that consumes another's output is not independent. |    |
| 4.17 | Report the batch count and what each batch covered, so a partial failure names the units that did not run | — | Report how many batches there were and what each covered, so a partial failure names exactly what did not run. |    |
| 4.18 | — | At most ONE agent per worktree: two agents in one checkout read each other's half-written files and neither can attribute or undo a change it did not make; not waivable by an agent's own reading of its brief, shared only when Gabe explicitly asks | At most one agent per working copy. Two agents in one copy each mistake the other's half-written files for the state they are reasoning about, and neither can tell which changes are its own. An agent's promise not to touch things is not a mechanism; sharing happens only when the owner asks for it. |    |
| 4.19 | — | A review or test of work in flight targets that branch in its OWN worktree of it — not the main checkout, not the worktree the effort agent is writing in; create one if the branch has none | An agent reviewing or exercising work that is still in progress gets its own working copy of that branch — not the shared one, and not the copy the working agent is writing into. Create one if it does not exist. |    |
| 4.20 | One worktree per campaign (any multi-commit effort); one-commit fixes and docs or preset tweaks run on the main checkout (overruled at review) | All work happens in a worktree, even for a lone agent; the main checkout keeps only conversation, adjudication and relay | ✓ ruled: All work happens in an isolated working copy of its own, however small the work is; there is no general size exception. The shared copy is reserved for a short, named list of operations that run there by convention: filing a dictated rule the moment it is dictated; merging a finished effort's branch into the shared line once every verification leg is green, and pushing it; and creating, listing and tearing down the working copies themselves. Anything not on that list gets a copy of its own. dropped nuance: the ruling's own form also carries a mechanical way to tell which copy an agent actually wrote in. | Gabe (chat 2026-09-01 18:54): remove the general exception that single commits can happen in main; only by-convention operations (e.g. real-time rule additions) happen in main. |
| 4.21 | — | Agents are named by CLASS and TARGET at spawn, because the harness shows only an opaque id and without the convention a live agent's class is invisible | Name each agent at spawn for the kind of work and the thing it is working on. The tooling shows only an opaque identifier, so without the convention you cannot see at a glance what is already running. |    |
| 4.22 | — | A mock stood up for a piece of work runs FROM that work's worktree, never the main checkout, never shared; a PARTIAL mechanism for teardown, since removing a worktree does not kill a process started from it | A stand-in service started for a piece of work runs from that work's own working copy, never the shared one and never shared between jobs, so it serves the code under test. This gives the process a visible owner but does not stop it: removing the working copy does not kill what was started from it. |    |
| 4.23 | Default assignments: haiku for enumerative extraction with citations, sonnet for synthesis and judgement subagent work, main-loop model only for design, adjudication and verdicts | — | Match the model to the kind of work: the cheapest tier for enumerating and extracting with citations, a mid tier for synthesis and judgement, and the main conversation's own model only for design, adjudication and verdicts. |    |
| 4.24 | Two-strike rule: when one participant's approach on a problem fails more than once, the collaborating participant's suggestion takes the next attempt — counted honestly and symmetrically | — | When one participant's approach to a problem has failed twice, the next attempt goes to the other's suggestion. The count is kept honestly and applies to both sides equally. |    |
| 4.25 | A structural change to a shared core type requires cross-lane sign-off, through the shared-seam register, BEFORE it lands | — | A structural change to something several parallel lines of work depend on gets sign-off from those lines, through the shared record that tracks that seam, before it lands. |    |

## 5. Worktree & Campaign Discipline

*When a separate working copy is required at all is settled at 4.20 and not
re-rowed here; who may occupy one is 4.18, where a review or test runs is 4.19,
and where a stand-in service runs is 4.22. This group starts once the copy
exists: naming it, basing it, working in it, committing from it, merging it,
and taking it down. The merge bar's own legs are already rowed — whole-suite
runs with untruncated output at 3.23, never rebaking a baseline at 3.30–3.31,
judging a comparison by its mismatch set at 3.29.*

| id | ferrislicer | dwc-ng | proposed universal form | |
|---|---|---|---|---|
| 5.1 | Branch name = the worktree name VERBATIM, no `worktree-`/`claude/` prefix; the WorktreeCreate hook enforces it for tool-created worktrees | — | Name the branch exactly what the working copy is named, with no added prefix, and let the tool that creates copies enforce it rather than trusting people to type it. | |
| 5.2 | `create_worktree.py` strips a leading `worktree-` the harness may have baked into the name, so branch and directory are both bare; an empty name aborts, printing the payload it was handed | — | The creating tool strips any prefix the harness added, so the directory and the branch come out bare, and it refuses a request with no name, saying what it was handed. | |
| 5.3 | Base ref from `worktree.baseRef`: `head` branches from local HEAD, `fresh` from `origin/<default>`; an unknown origin default falls back to HEAD rather than failing | — | Where a new working copy branches from is a project setting, not a guess: the current local position or the shared upstream default. When the upstream default cannot be determined, branch from the current position rather than fail. | |
| 5.4 | If the branch already exists the hook adds the worktree onto it instead of trying to create the branch again | — | When the branch already exists, attach the new working copy to it rather than trying to create it a second time. | |
| 5.5 | Contract: stdout is the created worktree's full path and nothing else, sub-command chatter is routed to stderr to keep it clean, and any non-zero exit aborts creation with stderr as the error | — | The creating tool prints the new copy's location on its output channel and nothing else, sends every other message elsewhere, and aborts creation on any failure with its own message shown as the reason. | |
| 5.6 | The mandatory FIRST step in every worktree is a hard reset onto the ACTUAL target branch: agent/isolation worktrees branch from `origin/<default>` regardless of the setting, so they land on the default branch, which lacks the target work; if local `main` leads origin, reset onto local `main` before any work or the campaign is built on a stale base | — | First act in a fresh working copy: reset it onto the exact branch the work targets — everything after builds on that tree. A fresh copy is not there: it opens on the project's default branch, and your own copy of that branch may be newer than the shared one, so reset onto whichever is newer. | Gabe (chat 2026-09-01 18:59): too vague — reworded. |
| 5.7 | About to make the SECOND commit on `main` for one continuous piece of work? You already needed a worktree — stop and create one before going further | Moot: work never starts on the main checkout at all (4.20) | If you are about to make a second commit on the shared copy for one continuous piece of work, you already needed a copy of your own. Stop and make one before going further. This is a backstop, not a licence for the first commit: under the ruling at 4.20 that one did not belong there either. | |
| 5.8 | Commit with explicit pathspecs (`git commit -- <paths>`); a bare commit takes the whole index, and the shared index has swept unrelated files in before — the repo root routinely carries scratch models, output and images that must never land | — | Every commit names the exact paths it is committing. A commit that just takes whatever is staged sweeps in whatever else was lying around, and that has put unrelated files into commits before. | |
| 5.9 | `-m` goes BEFORE `--`; everything after `--` is a pathspec, including a `-m` that strays there | — | Put the message and its flags before the path separator on a commit command. Everything after that separator is read as a path, including a flag that strays there. | |
| 5.10 | Stage only the specific named files a change touches; never `git add -A` or any blanket add, which risks re-tracking ignored scratch content | — | Stage the named files a change touches, never a blanket add-everything, which quietly re-tracks ignored scratch content. | |
| 5.11 | Never stage protected tracking files — `USER_AUDIT.md`, `SLICER_FEATURE_TODO.md`, `AGENTS.md`, `.superpowers/` | — | Some tracked files are the owner's own working record and are never staged by an assistant, whatever else the change touches. | |
| 5.12 | Every commit message ends with the standard AI co-author attribution trailer | — | Every commit message ends with the standard trailer attributing the assistant's part in it. | |
| 5.13 | Credit Gabe BY NAME for any idea, diagnosis, algorithm or fix that originated with him — in the commit message AND in the code comment | — | Credit the person by name for any idea, diagnosis, algorithm or fix that came from them, in the commit message and in the code comment. | |
| 5.14 | `scripts/sweep_guard.sh`, advisory-only in pre-commit (always exit 0): a docs-shaped staged set containing a NEWLY-TRACKED non-doc file prints its denominators — staged, newly-tracked, suspects — and names each suspect; a deliberate mixed commit keeps it silent | — | Backstop for 5.8: fires when a docs-shaped commit also adds a brand-new non-documentation file, naming it without blocking. It goes silent only if the commit also modifies an existing non-documentation file elsewhere — that is an openly mixed commit, not a sweep. dropped nuance: which paths count as documentation; a merely-changed tooling file is tolerated. | Gabe (chat 2026-09-01 19:03): don't understand it — rewritten. |
| 5.15 | Do not start the merge until every leg passes — workspace tests, goldens, oracle parity. Green on one leg is not green | No stated merge bar; the stated sequence is work lands, then review comes back clean, then the hands-on gate (7.4) | Merge only after every required verification leg has passed. Passing one leg is not passing. What being driven against a stand-in of the real target adds to the bar is 7.4. | |
| 5.16 | Working copies share and poison Cargo's incremental cache; a `pub` symbol "not found" or a signature mismatch whose reported line is not the real `fn` means a stale linked build — `cargo clean`, rebuild, and only then call it a real breakage | — | Working copies share one incremental build cache and can poison it. When a build error contradicts what the source plainly says, clear the cache and rebuild before treating the error as real. | |
| 5.17 | Research branches (`performance_research`, `total_research`) are NEVER auto-merged | — | Branches kept for exploration are never merged automatically. Merging one is always a deliberate decision. | |
| 5.18 | Immediately after the merge, in the same session: remove the worktree and delete the branch. Each carries its own `target/`; skipping this has put ~230 GB on disk. The merge is not the end of the campaign, the teardown is | — | Delete the working copy and its branch immediately after the merge, in the same session. Each copy carries its own build output, and stale ones have run to hundreds of gigabytes. The merge is not the end of the effort; the teardown is. | |
| 5.19 | Before you leave, `git worktree list`. Anything listed that is not an active campaign is debt from a previous one — report it, never silently remove someone else's | — | Before you finish, list the working copies that exist. Anything left that is not active work is debt: report it, and never quietly delete someone else's. | |
| 5.20 | Never remove any worktree with uncommitted changes without asking first | — | Never delete a working copy that has uncommitted changes without asking first. | |
| 5.21 | `.perf-worktrees/` slots are managed by `scripts/perf.sh worktree-rm <slot>`; never a raw `git worktree remove` while a measurement holds the lock | — | A working copy that some tool holds a lock on is released through that tool, never removed by hand while it is still in use. | |
| 5.22 | Inside a worktree, anything above the repo root is off limits unless asked for | — | Inside a working copy, anything above the project root is off limits unless you were asked to touch it. | |
| 5.23 | A shared worktree is SHARED STATE: a session writing TEMPORARY instrumentation announces the file and a unique marker string BEFORE writing, and removes both when the measurement ends | — | A working copy someone else can see is shared state. Announce temporary measuring code before you write it — the file, and a unique marker to find it by — and remove both when the measurement ends. Unannounced, live instrumentation is indistinguishable from unexplained drift, and the honest response to unexplained drift is to revert it, which destroys the measurement mid-run. | |
| 5.24 | `refresh-diverged-branch`: two parallel editions diverged so far that a merge is a wall of conflicts, OR that both lines independently rewrote the SAME shared subsystems (add/add conflicts on files both sides created), are re-synced by rebuilding the downstream edition on fresh upstream instead of merging; the cheap sync path stays the default until it stops being worth it | — | The edition branch follows the primary line, never the reverse. When merging it throws a wall of conflicts, or both branches independently rewrote the same shared parts, rebuild the edition on a fresh copy of the primary line and re-apply only its genuine delta instead of merging. Ordinary syncing stays the default until it costs more than that rebuild. | Gabe (chat 2026-09-01 19:07): trailing vs leading ambiguous — reworded; naming made consistent across 5.24–5.38. |
| 5.25 | Always categorize from the MERGE-BASE diff, never the direct-tip diff: a tip diff marks a file as added even when both sides added it independently, hiding an add/add conflict as an upstream-only file | — | Work out what each side changed by comparing both against their common ancestor, never by comparing the two tips. A tip comparison reports a file as newly added when both sides added it independently, which hides a real collision as a one-sided change. | |
| 5.26 | Split the change sets: files only downstream touched re-apply VERBATIM (upstream == base, so a three-way merge would return downstream's version anyway); files both touched are the true reconciliation set | — | Split the two change sets in two: what only the edition branch touched re-applies word for word, because the other side never moved from the common ancestor; what both touched is the only work that needs a decision. | |
| 5.27 | Triage the both-changed set by churn DIRECTION, not presence: upstream a superset (huge deletions, tiny additions — downstream trails by thousands of lines) means keep upstream and graft the small platform hook; balanced churn means genuine divergence, re-apply the delta by hand | — | For a file both sides changed, decide by which way the change ran, not by the fact that both touched it. Where one side is simply far ahead, keep its version and graft back only the small hook the other side added; where both changed comparably, it is real divergence and gets reconciled by hand. | |
| 5.28 | Same triage, by file kind: docs (CLAUDE.md, ADRs) merge both sides' additions; lockfiles (`Cargo.lock`) are regenerated, never hand-merged | — | Two kinds of file skip that judgement: documentation takes both sides' additions, and a generated lock file is regenerated rather than hand-merged. | |
| 5.29 | Build the refresh branch AS upstream (`git switch -c <downstream>-refresh <upstream>`), so every "keep upstream" file is already correct and needs no action; then check out the downstream-only files verbatim | — | Start the rebuilt branch as a fresh copy of the primary line, so every file you decided to keep from it is already right and needs no action at all, then re-apply the one-sided files word for word. | |
| 5.30 | For each real both-changed hook the platform intent is the `merge-base..downstream` diff; apply only the small identifiable blocks onto upstream's CURRENT version, which may have refactored around them | — | Graft only the small identifiable blocks that carry the edition branch's intent, onto the primary line's current version — which may have been reorganized around them. Do not replay the whole file. | |
| 5.31 | Non-negotiable: a file existing only on the downstream line is re-homed into that platform's OWN crate, never restored into a shared crate behind `#[cfg]` — trait in the shared crate, concrete impl injected from the platform crate, consumers' imports updated. That is what keeps the NEXT refresh cheap | — | A file that exists on only one edition is re-homed into that edition's own module, never restored into shared code behind a conditional-compilation switch: the shared side keeps the interface, the edition supplies the implementation. That is what stops the file being a shared-file conflict at every future refresh. | |
| 5.32 | Verify the refresh branch before promoting: host tests and lint, plus the platform build for the downstream target | — | Verify a rebuilt branch before promoting it: the ordinary tests and checks, plus a build for the edition's own target. | |
| 5.33 | Smoke-test on device/emulator when shared GUI or engine code moved — a clean graft can still change rendered behaviour no host test catches | — | Exercise a rebuilt branch by hand on real hardware when shared interface or engine code moved. A clean graft can still change what a person sees, and no test that runs on your own machine catches that. | |
| 5.34 | Verify that upstream's added features kept their unit test, fixture and integration coverage through the rebuild | — | Confirm the features the primary line added still carry their own tests after the rebuild, since a rebuild can quietly drop the coverage that came with them. | |
| 5.35 | Promote the refresh branch to the real downstream branch as a SEPARATE, deliberate step, only when it is production-worthy. Never auto-merge | — | Promote a rebuilt branch onto the real one as a separate, deliberate step, and only when it is production-worthy. Never automatically. | |
| 5.36 | No remote? The method needs none: trust a local `upstream` you just built and re-apply by hand — the fetch-guarded sync script is unusable without fetch access | — | With no access to the remote this method still works against a local copy of the primary line. The guarded sync script does not, so do not reach for it. | |
| 5.37 | rerere still helps if you later fall back to merging: resolutions recorded on one chase replay on the next; a refresh sidesteps the conflicts entirely | — | Keep conflict-resolution recording on. If you ever fall back to merging, the resolutions from one chase replay on the next; the rebuild simply avoids the conflicts instead. | |
| 5.38 | Delete stale build caches first, not branches: a diverged worktree's disk is almost all rebuildable `target/`, and removing it reclaims the space without touching any branch or source | — | To reclaim disk from a stale working copy, delete its rebuildable build output first. That is nearly all of the space, and it costs no branch and no source. | |
| 5.39 | Name the worktree for the CAMPAIGN, not for a date or a ticket | — | Name a working copy for the work it holds, not for a date and not for a ticket number. The name is how anyone else tells what is running in it. | |
| 5.40 | A campaign starts only on the owner's explicit go, given DIRECTLY to the agent that will run it; a relayed authorization never counts | — | A multi-commit effort starts only on the owner's explicit go, given directly to whoever will run it. An authorization passed along by somebody else does not count. | |

## 6. Work Tracking

*Only one project has a ticket protocol; the other tracks work in an
engineering notebook whose contract it shares word for word, which is why
6.24–6.27 are the rows with two filled columns. The single-editable-home
machinery for the rule text is rowed here; the gate behaviour it leans on —
printing its own count line, going red rather than skipping — is 8.21 and
8.31. That the generated file can still be hand-edited between commits, and
that only the commit gate keeps that out of history, is an
enforcement-honesty statement of the shape 2.7 and 2.21 already demand. Rules
extracted from notebook entries reach their durable home by 2.9–2.11.
6.29–6.35 were added by the coverage sweep: the owner's own bug list, and the
method rules for a pass over material being documented — the work of recording
what is there, as distinct from tracking the work of changing it. When such a
document must be updated is 13.27–13.30. 6.36 is an owner ruling added at
review: the link that binds a ticket to its companion must be a different KIND
of relationship from the ordering and dependency links between an effort's
tickets and the work under it, so a query discriminates the two by relationship
type rather than by remembering field values.*

| id | ferrislicer | dwc-ng | proposed universal form | |
|---|---|---|---|---|
| 6.1 | Related bugs share one worktree; discover the link late, combine and clean up | — | Related defects are worked in one place. Discovering the link late means combining them and cleaning up, not carrying two efforts. | |
| 6.2 | Every new piece of work gets a GitHub issue, engineer-stranger compatible, a full engineer spec; `/superpowers` plans and specs hang off it | — | Every new piece of work gets a ticket written so a competent stranger could pick it up cold, and the plans and specifications for it hang off that ticket. | |
| 6.3 | Every ticket has exactly ONE "Context:" sub-issue: the compressed pickup context for an AI session | — | Every ticket has exactly one companion entry holding the compressed pickup context for an assistant starting fresh. | |
| 6.4 | Catching up: list issues for titles, fetch the ticket's one sub-issue, read its description — nothing else unless that proves insufficient | — | To catch up on a ticket: list the titles, fetch its one companion entry, read that. Nothing else, unless that turns out to be insufficient. | |
| 6.5 | Keep the context sub-issue CURRENT as the ticket moves | — | Keep the companion entry current as the ticket moves. A pickup context that describes last week is worse than none. | |
| 6.6 | The sub-issue relation is RESERVED for this pair alone; nothing else is ever a sub-issue of anything | — | The parent-and-companion relationship is reserved for that pair alone. Nothing else is ever filed as a child of anything — see 6.36 for why the mechanism must be distinct. | |
| 6.7 | Campaign findings, epic chapters and follow-ups are each their OWN new ticket pair — full parent plus its own Context child — never a sub-issue of the campaign | — | A finding, a chapter of a larger effort, and a follow-up are each their own new ticket pair, never filed underneath the effort's ticket. | |
| 6.8 | Added-links mirror at BOTH levels: new parents are "added" tickets on the overall parent, new Context children "added" on the overall Context child, so a resuming session reaches every finding's context from the one campaign sub-issue without pulling a full parent | — | Link a new pair to the effort at both levels — the full ticket to the effort's ticket, the companion entry to the effort's companion entry — so a session resuming the effort reaches every finding's context through companion entries alone, without ever opening a full ticket. | |
| 6.9 | Issue reads are minimum-token: use the API to fetch only the fields needed; a full issue read only when more context is needed; never pull full issues wholesale | — | Read the tracker for the fields you actually need. Read a whole ticket only when the short context proves insufficient, and never pull whole tickets in bulk. | |
| 6.10 | Learnings are added to the sub-issue as AI-condensed context | — | Learnings are written back into the companion entry, condensed, so the next reader gets them at pickup cost. | |
| 6.11 | A referred issue number ALWAYS means the SET (parent + child); the access rules still apply, and the referred number does not redirect | — | When someone names a ticket number they mean the pair, whichever half they named. The reading rules still apply, and the number you were given does not send you somewhere else. | |
| 6.12 | A correction from the developer updates the parent full spec AND the child context, every time | — | A correction from the owner updates both halves of the pair, every time. | |
| 6.13 | Every GH issue gets a label for the worktree it is in — the same label on BOTH halves of the pair | — | Every ticket carries a label naming the working copy the work lives in, on both halves of the pair. | |
| 6.14 | Every commit message carries a marker for its issue, in the form `GIT_\d+` | — | Every commit message carries a marker naming the ticket it belongs to. | |
| 6.15 | `new_ticket_pair.py` is the ONE pipeline: create the parent, create the child titled `Context: #<parent>`, link them with the real sub-issues API (not a mention), apply the same labels to both. By hand it is three calls in a fixed order keyed on a numeric id nobody can see in the UI, so it gets done differently each time and pairs end up half-made | — | Creating a ticket pair is one command, not a sequence people repeat by hand: it creates the ticket, creates its companion titled for the ticket, links them with the real relationship rather than a mention, and labels both the same. Done by hand it is several calls in a fixed order keyed on an identifier the interface never shows, so it gets done differently each time and pairs end up half-made. | |
| 6.16 | The same script repairs a half-made pair: `--link PARENT CHILD` attaches an existing child to its parent | — | The same command repairs a half-made pair by linking two tickets that already exist. Repair goes through the pipeline too, not around it. | |
| 6.17 | Parent body shape: Problem, Required behaviour (numbered), Design constraint (cant-break-by-design), Decided (dated, attributed to Gabe), Tests required, Sites (file:line) | — | A full ticket has a fixed shape: the problem, the required behaviour as numbered items, the design constraint, the dated decision and who made it, the tests required, and the exact places in the code it touches. | |
| 6.18 | Child body shape: "Context for the parent (AI pickup)." then 5-8 bullets — ruling, today's cause with file:line, shape of the fix, decisions, tests, worktree name | — | A companion entry opens by saying it is the pickup context, then gives five to eight bullets: the ruling, today's cause with its exact location, the shape of the fix, the decisions, the tests, and the working copy it lives in. | |
| 6.19 | `needs-input` on BOTH halves when the pair is blocked on a decision from Gabe, plus a comment on the child saying exactly what input is needed; the autonomous pass skips it | — | When a pair is blocked on the owner's decision, label both halves as blocked and comment on the companion entry saying exactly what input is needed. An unattended pass skips anything so marked. | |
| 6.20 | Close-out order: close the parent with a comment naming the merge commit, add a short AI-condensed learnings comment on the child, then close the child | — | Close a pair in order: close the ticket with a comment naming the change that landed, add the condensed learnings to the companion entry, then close it too. | |
| 6.21 | The rule text has ONE editable home (`docs/rules/tracking-work.md`, between drift-gate markers); `docs/github-issue-rules.md` is rendered from it plus a template that holds no rule text, so there is no second home a divergent copy could ever be typed into | — | A rule that gets restated elsewhere has exactly one editable home, and every restatement is generated from it through a template that carries no rule text of its own. There is then no second place a divergent copy can be typed. | |
| 6.22 | `gen_issue_rules_doc.sh --check`, blocking in pre-commit: the artifact must be byte-identical to a fresh render; on mismatch it REGENERATES in place and STILL exits non-zero, so the fix is to review and re-stage, never to hand-edit | — | A blocking check compares the generated copy against a fresh render and requires them to be identical. On a mismatch it rewrites the file correctly in place and still fails, so the fix is to review and re-stage the regenerated file, never to hand-edit it. | |
| 6.23 | `--self-test` red-check fixture: perturbs the REAL artifact and the REAL authority in place (there is no second copy to perturb — that is the point), restores each byte-for-byte after its sub-test, and verifies the working tree is unchanged afterwards | — | A generator that leaves only one copy of anything must test itself against that copy: disturb the real file, watch the check go red, restore it exactly, and prove afterwards that the working tree is unchanged. Having no spare copy is the point, not an obstacle. | |
| 6.24 | Learnings ship with the major commit or worktree, versioned like code; the notebook's dated entries are appended in the same commit as the work that produced them | Identical contract: dated entries appended in the same commit as the work that produced them, managed like any source file, and the file's history is the record of how understanding evolved | Learnings ship with the work that produced them — written in the same change, versioned and managed like source — so the history of the record is itself the record of how understanding changed. | |
| 6.25 | Each notebook entry is self-contained: what prompted it, what was done, what was observed, what we conclude | Identical entry contract, word for word | Every entry stands on its own: what prompted it, what was done, what was observed, and what is concluded. | |
| 6.26 | Each campaign additionally keeps a ledger of stats and changes: what we did, what changed, what is better, what regressed, whether the refactor worked, new smells | — | Every effort also keeps a running ledger: what was done, what changed, what is better, what got worse, whether the restructuring achieved its point, and what new smells appeared. | |
| 6.27 | Division of the record: defect status lives in GitHub issues, specs and plans state intent, the notebook states what reality answered; work is fully recorded only when both sides exist | Identical division, word for word | Three records, three jobs: the tracker holds what is broken, the plans and specifications hold what was intended, and the notebook holds what reality answered. Work is fully recorded only when both intent and answer exist. | |
| 6.28 | The generated restatement adds what the verbatim rule lacks — the commands that execute it — so another session can follow it without the repo context | — | A restatement of a rule earns its place by adding what the rule text lacks: the exact commands that carry it out, so someone with no context can follow it. | |
| 6.29 | Do not mark an item on the owner's own bug list fixed until the owner has confirmed the behaviour VISUALLY | — | An item on the owner's own list of complaints is marked fixed only when the owner has seen the behaviour and said so. Your own verification does not close it. | |
| 6.30 | Do not rewrite the descriptions in the owner's bug list | — | Never rewrite the owner's own description of a problem. It is their record of what they saw, and editing it destroys the only account of the symptom that is not yours. | |
| 6.31 | Each mapping pass is shallow enough to FINISH IN ONE SITTING: ragged — one part written deep beside many left blank — is worse than uniformly shallow | — | A pass over material to be documented is shallow enough to finish in one sitting. Uniformly shallow beats ragged: one part written deep beside many left blank tells a reader nothing about which gaps are real. | |
| 6.32 | Every row of a mapping or analysis document carries its SOURCE LOCATION, so continuation costs nothing and a claim can be rechecked when either side moves | — | Every row of a mapping or analysis document carries where it came from. Continuing the work then costs nothing, and any claim can be rechecked when either side moves. | |
| 6.33 | NO defect may be used as the organizing principle for a reading pass: steps are covered in the pipeline's own order, never in the order a bug makes them look relevant | — | A defect is never the organizing principle for a reading pass. Cover the material in its own order: reading it in the order a bug makes things look relevant produces a map of that bug, not of the system. | |
| 6.34 | A pass DECLARES a depth limit up front and records only what that pass verified; an unverified fact is left ABSENT, never guessed — in prose and in the models alike | — | A pass states its depth limit before it starts and records only what it actually verified. An unverified fact is left absent rather than guessed, in the prose and in any model built beside it, because a guess in a reference document is indistinguishable from a finding. | |
| 6.35 | Forward and backward analysis directions ADVANCE TOGETHER as one artefact at one depth; a pass is not complete if only one direction progressed | — | Where an analysis runs in two directions, both advance together as one artefact at one depth. A pass with only one direction moved is not a completed pass. | |
| 6.36 | "The sub-issue relation is reserved for this pair alone. Nothing else is ever a sub-issue of anything" (mechanism-distinctness reason added by owner at review) | — | ✓ ruled: The link that binds a ticket to its companion must be a different KIND of relationship from the links that express ordering or dependency (blocked-by, depends-on) between an effort's tickets and the work tickets under it — different enough that a query can tell the two apart by the relationship's own type, never by remembering which values of a shared field mean which. Correct by construction: the tracker cannot confuse a companion with a dependency. | Gabe (chat 2026-09-01 19:23): added at review. |

## 7. Test Double & UAT Discipline

*This group is one project's alone; the other proves changes against golden
and comparison suites, which are domain machinery, so its column is empty
throughout. Its nearest equivalents are already rowed: a layer no unit test
can honestly reach is verified by build, lint and a hands-on run (3.28), and
every verification leg must be green before a merge (5.15). Where the stand-in
process runs and who owns it is 4.22. Nothing here is enforced by a machine —
the teeth are that a completion claim with no note of what was driven fails on
its face, which is reviewable by eye where "did you run it" is not.*

*Owner ruling, 2026-09-01: this whole group is web-app-specific and is
dropped from the union. All eight rows below are marked `✗ not in union:
`; phase 3 does not carry any of them forward. The rows and their
per-project cells remain here as the record.*

| id | ferrislicer | dwc-ng | proposed universal form | |
|---|---|---|---|---|
| 7.1 | — | The full mock suite runs DURING development — keep `pnpm mock` up while building UI — and a user-facing change is not done until it has been driven against mock-duet. A green unit suite is not UAT: it exercises the units you wrote a fixture for, not the wiring a person touches | ✗ not in union: Keep a stand-in for the real target running while you build, and treat a user-facing change as unfinished until you have driven it against that stand-in by hand. A green test suite is not the same evidence: it exercises the pieces someone wrote fixtures for, not the wiring a person actually touches. | |
| 7.2 | — | A completion claim RECORDS the UAT: what was driven, against which scenario, what was observed. No note means the change is not done | ✗ not in union: A claim that something is done states what was driven, against which scenario, and what was observed. With no such note, the change is not done. | |
| 7.3 | — | A change to what the UI reads from or writes to the board — a new object-model key, file path, config version, endpoint — updates `packages/mock-duet` in the SAME change. Mock parity is part of the work, not a follow-up ticket | ✗ not in union: A change to what the product reads from or writes to the real target updates the stand-in in the same change, never as a follow-up. A stand-in a version behind cannot host the hands-on run everything else depends on, so letting it drift disables the rule that catches everything else. | |
| 7.4 | — | Nothing deploys to the printer until Gabe has UAT'd it on the mock. Sequence: work lands, review clean, mock stood up and handed to him, HE drives it, he says deploy. A clean review is not permission to ship | ✗ not in union: Nothing reaches the real target device until the owner has driven it on the stand-in himself. A clean review is not permission to ship: the implementer's own run is evidence that it works, the owner's run is the gate that lets it reach hardware. | |
| 7.5 | — | EVERY code-complete iteration is deployed to the mock and announced — not only the final one, not gated on review being clean — so Gabe can drive it while review and fix rounds continue | ✗ not in union: Stand every code-complete iteration up on the stand-in and say so, early and often, without waiting for the review to come back clean. Saving the hands-on run for the end is how a misread requirement survives three rounds of fixes before anyone who knows the real thing looks at it. | |
| 7.6 | — | Whoever stands a mock up owns tearing it down: an iteration's mock stops when that UAT ends, a ticket's mock does not outlive the ticket's merge. Falsifying check: a process scan filtered to `mock-duet` returns nothing older than the current session's work | ✗ not in union: Whoever starts a stand-in owns stopping it. One started for a round of exercise ends with that round, and one started for a piece of work does not outlive its merge. The check is a process scan that returns nothing older than the current session's work. | |
| 7.7 | — | Confirm a kill by PORT STATE, never by exit code — `pkill` exits 0 on Windows while leaving the process alive | ✗ not in union: Confirm that you actually stopped something by looking at whether it is still listening, never by the exit status of the command you used to stop it. A stop command can report success and leave the process alive. | |
| 7.8 | — | Identify the mock you are driving by owning PID and start time (`Get-NetTCPConnection` cross-checked against `CreationDate`), never by "something answered on that port": a failed start still saw a healthy `rr_connect` reply from the previous evening's orphan, one step from a UAT that would have validated the wrong code | ✗ not in union: Before reporting a stand-in as running, confirm the process listening there is the one you just launched — its identifier and its start time. Something answering on the expected address is not evidence that the expected process produced it, and a leftover one may also be running with limits raised or authentication turned off, quietly disabling the very checks the exercise exists to apply. | |

## 8. Tool Output Hygiene

*All of this is one project's; the other has no output-filtering machinery, so
its column is empty throughout. These rows state observable behaviour rather
than intent, because they are the specification the union's hook has to
satisfy. 8.31's source is the generated-document gate rowed at 6.21–6.23.*

| id | ferrislicer | dwc-ng | proposed universal form | |
|---|---|---|---|---|
| 8.1 | PreToolUse hook on `Bash`/`PowerShell`: reads the tool payload and, when the command matches a known-noisy shape, rewrites the tool input to run under `quiet_run.py`, appending `[quiet:<mode>]` to the description. The command's exit code is preserved | — | Before a shell command runs, a check looks at it and, when it matches a known-noisy shape, rewrites it to run under a wrapper that keeps the whole output on disk and shows only what matters. The exit status passes through unchanged, and the rewritten call is labelled so it is visible that filtering happened. | |
| 8.2 | Fails open: an unparseable payload, a tool other than `Bash`/`PowerShell`, or no pattern match all return 0 with no output, so the command runs exactly as written; the hook runs under a 15-second timeout | — | The check fails open. A payload it cannot read, a tool it does not handle, and a command that matches nothing each leave the command exactly as written, and it runs under a time limit so a stuck check can never block work. | |
| 8.3 | `filter` mode for builds, installs, test runs, package and container tooling, toolchain updates, the named shell gates, any `*_test.py` under `scripts/` or `.claude/hooks/`, and run-log/check chatter: reduced to error blocks, summaries and the tail | — | Commands whose payload is a pass-or-fail verdict — builds, installs, test runs, package and container tooling, gate scripts, test runners, log-viewing chatter — are reduced to their error blocks, their final summaries and their last few lines. dropped nuance: what counts as one of these is an explicit, deliberately narrow list of command shapes rather than a general rule; 8.10 says when a list is the right answer. | |
| 8.4 | `infra` mode for version-control state changes (commit, push, pull, fetch, merge, rebase, clone, cherry-pick, worktree add/remove/prune, submodule) and tracker mutations (pull-request and issue create/edit/comment/merge, workflow run, release, repo and label operations): show ONLY the lines that prove success, even when the output is short. Checked BEFORE the noisy set: `git clone`/`fetch`/`pull` appear in both, and infra wins — asserted by tests on `git pull --rebase` and `git fetch origin` | — | Commands that change state somewhere else — committing, pushing, pulling, merging, creating or editing a ticket — are reduced to the lines that prove the action succeeded. This applies even when the output is already short, because that chatter is always short and still never worth reading. A command that is both state-changing and noisy is treated as state-changing: cloning, fetching and pulling are in both sets, and proving they worked is what matters. | |
| 8.5 | On a non-zero exit, `infra` mode falls back to the normal error/summary selection so nothing about the failure is lost | — | When one of those state-changing commands fails, the reduction switches to the full error-and-summary selection, so nothing about the failure is lost. | |
| 8.6 | If a successful `infra` run matched no proof line, the LAST line is shown — the selection is never empty | — | If a successful action produced no line the wrapper recognizes as proof, its last line is shown anyway. The result is never nothing. | |
| 8.7 | Content reads — issue and pull-request view/list/diff/status, `api`, `search`, release/run/repo/label/project/gist/workflow reads — are NEVER wrapped: their lines are the payload, never capped, never filtered. A read anywhere on the command line wins over any noisy match on the same line | — | A command whose output IS the thing you asked for — a ticket, a diff, a listing, an interface response — is never wrapped at all, so nothing filters it and nothing shortens it, including the cap in 8.32. A read anywhere on the command line wins over anything noisy on the same line. | |
| 8.8 | Analysis, probe, render, census and diff scripts are deliberately excluded even though they sit beside the wrapped ones in the same directory; the negative tests matter more, since a regression there starts filtering data the caller asked to see | — | A tool whose printed output is the data the caller wanted is never wrapped, even when it sits right beside tools that are. The tests proving these stay unwrapped matter more than the ones proving wrapping works, because a mistake in this direction silently deletes what somebody asked for. | |
| 8.9 | Left untouched: a command already piped into `tail`/`head`/`grep`/`jq` and friends, output already redirected to a file (merging stderr into stdout does not count), a command already under the wrapper, and `--version`/`--help`. Piping wins even over the noisiest gate | — | A command that already handles its own output is left alone: piped into something that trims it, redirected to a file, already running under the wrapper, or merely asking for a version or help. Merging error output into normal output is not a redirect and does not exempt anything. Piping wins even over the noisiest command, because the user's own post-processing is never overridden. | |
| 8.10 | The shell gates share no prefix, suffix or behavioural marker a regex could key on, so they are an explicit enumeration; test runners DO share a `*_test.py` suffix under two directories — a real regularity — so that arm is a suffix match and covers new test files without editing the hook again | — | Match on a real naming regularity wherever one exists, so new files are covered without editing the matcher, and fall back to naming things one by one only where no regularity holds. Say in the matcher which of the two you are doing and why. | |
| 8.11 | Per-call opt-out: prefix the command with `FS_QUIET=0` (or the PowerShell equivalent) and `quiet_run.py` prints everything verbatim | — | A single call can opt out with an environment prefix, and the wrapper then prints everything word for word. | |
| 8.12 | In `filter` mode, output at or below `PASS_THROUGH_LINES` (40) is printed verbatim — quiet commands are not filtered. `infra` mode filters regardless of length | — | In the reducing mode, output at or below a fixed small number of lines is printed word for word: a quiet command is not filtered at all. The proof-of-success mode still reduces, however short the output. | |
| 8.13 | Everything is streamed to a timestamped log file; the header states mode, exit code, elapsed time, `<produced> lines -> <shown> shown`, and the log path | — | The whole output is always written to a log, and the header of what you do see states the mode, the exit status, how long it took, how many lines there were against how many are shown, and where the full log is. Printing that count is the same demand 8.21 makes of a gate. | |
| 8.14 | Before selection: line endings normalised, a progress bar reduced to the frame after its last carriage return, escape sequences stripped, trailing blank lines dropped | — | Output is normalised before anything is selected: a progress bar collapses to its final frame, colour and cursor codes are stripped, and trailing blank lines are dropped. | |
| 8.15 | Output stream reconfigured to UTF-8 with replacement, because the console defaults to a narrower encoding while tool output is UTF-8: never crash on output, substitute | — | The wrapper never fails because of a character it cannot print. Unrepresentable characters are substituted, because losing the whole output to an encoding error is worse than losing one glyph. | |
| 8.16 | The child runs with progress bars, colour, pagers, update notifiers and interactive prompts disabled by environment, stdin closed, stdout and stderr captured together | — | The wrapped command runs with progress bars, colour, pagers, update notices and interactive prompts turned off, and with no input attached, so there is less noise to filter in the first place and nothing can sit waiting for an answer nobody will type. | |
| 8.17 | Kept by `select()`: an error/panic/traceback/FAILED block from its start line to the next blank line, single lines matching failure keywords plus two lines of context, final summary lines, the last 8 lines, and any proof line | — | Whatever else is dropped, these always survive: an error, panic or failure block from its opening line to the next blank line; any line naming a failure, plus the couple of lines after it; final summary lines; the last few lines of output; and any line in the declared proof format. | |
| 8.18 | `CHATTER` lines — compiling, downloading, installing, `test ... ok`, unused-variable warnings, diagnostic gutter lines — are dropped even when they match a keyword, and are excluded from the tail window except the final line | — | Lines that are pure progress chatter are dropped even when they contain a word like "warning", and they do not get to fill the tail window either. The very last line is always kept regardless. | |
| 8.19 | `PROOF_LINE` matches two shapes only: a heartbeat line, or `<snake_case_tool>[ --flag]: <text>`. Anchored to FORMAT, never to an enumeration of gate names, so a fifth gate nobody named survives with no filter edit — pinned by a test using an invented tool name | — | The one pattern that keeps a tool's own proof line alive is anchored to the FORMAT of that line, never to a list of tool names, so a new tool's line survives with no change to the filter. A test pins this using a tool name that appears nowhere in the code. | |
| 8.20 | Requiring an underscore in the identifier is what keeps prose out — `remote:`, `warning:`, `note:`, `hint:` have none — MEASURED against real bulk output to confirm the pattern stays narrow | — | The proof format is narrow enough that ordinary prose does not match it, and that narrowness is measured against real bulk output rather than assumed. A pattern that admits every "word colon" line has stopped filtering. | |
| 8.21 | A merge or CI gate script that redirects a sub-check's output to a log echoes at minimum that sub-check's own denominator line to its own stdout, not only pass/FAIL | — | A gate that runs sub-checks and sends their output to a log still prints each sub-check's own count line on its own output. Pass or fail alone is not enough: a failure stays visible anyway, and a pass for a bad reason is exactly the thing that gets compressed away. | |
| 8.22 | The test framework prints its tally to stderr in a shape matching no keep-pattern, so it was silently eaten; the suite now prints its own conforming proof line carrying the denominator on stdout, IN ADDITION TO the framework's report | — | When a tool's own summary does not match the declared proof format, it prints an additional conforming line carrying its count — in addition to its usual report, never instead of it. Otherwise its result is silently eaten by the filter. | |
| 8.23 | A long-running tool emits ONE line at a bounded, regular interval matching a single project-wide pattern, so ONE expression recognizes every tool's heartbeat: which tool, that it is alive, and progress if knowable — a denominator beats a bare pulse | — | A long-running tool prints one line at a bounded, regular interval while it is still working, in a single shape used across every tool so one pattern recognizes them all. It names the tool, shows it is alive, and gives progress where progress is knowable, because a count beats a bare pulse. | |
| 8.24 | Written to the TOOL's own stdout, UPSTREAM of any log redirect — a heartbeat inside a swallowed redirect reaches no one | — | The heartbeat goes to the tool's own output, upstream of any redirect into a log. One written inside the redirect reaches nobody. | |
| 8.25 | Fixtures pin it: a synthetic heartbeat buried in forty lines of chatter, far outside the 8-line tail window, survives BOTH selection paths | — | A heartbeat survives the filter in every mode, including when it is buried in bulk chatter far outside the tail window, and a fixture proves it. | |
| 8.26 | Silence past one interval = dead or hung, never "probably still working". An agent watching a background job watches for the pattern; it never idle-waits for a completion signal it was never promised | — | Once the shape is standard, silence means something: no heartbeat within its interval means the job is dead or hung, not that it is probably still working. Watch for the pattern, and never sit waiting for a completion signal nobody promised you. | |
| 8.27 | A tool that runs past one heartbeat interval without emitting one is ITSELF a defect, on the same footing as a sub-check that emits no denominator | — | A long-running tool that goes past one interval without emitting a heartbeat is itself a defect, on the same footing as a check that reports no count. | |
| 8.28 | Negative control pinned: 300 lines of build chatter with no proof-shaped or heartbeat-shaped line must not come back wholesale just because a proof-line pattern now exists — under 10 kept lines in one mode, under 20 in the other | — | A change that lets more lines survive ships with a negative control proving bulk noise still does not: a few hundred lines of ordinary chatter with nothing proof-shaped in it must still reduce to a handful. | |
| 8.29 | The filter's own unit tests run BLOCKING in pre-commit and as a merge-gate backstop, because that is where a filter regression's consequence lands — these hooks shape every tool call's local output — and the suite runs in ~0.007s, so cost is not a factor | — | The output filter's own tests run blocking at commit time, because that is where a regression in them actually lands: from then on it shapes every command's output. The merge gate is the backstop for a copy that never installed the commit hook. | |
| 8.30 | No `-q` flags anywhere in a gate script: under the quiet hook, `-q` deletes the proof line | — | Do not pass a tool its own quiet flag when it runs under the output filter. The filter already decides what is shown, and the quiet flag deletes the very proof line the filter was going to keep. | |
| 8.31 | A missing template, a missing authority file, or a missing marker in either is its own RED path naming the gap — never a silent skip, because a skipped check reads as a pass | — | A check that cannot run fails loudly, naming exactly what it could not find. It never skips quietly, because a skipped check reads as a pass. | |
| 8.32 | `render()` caps the shown set at `MAX_SHOWN` (200): beyond it, a 3/5 head plus a 2/5 tail are shown with `... [N kept lines elided between head and tail] ...` between them, and every gap between non-adjacent kept lines is marked `... [N lines omitted] ...` | — | Even the surviving lines have a fixed ceiling. Past it, a leading and a trailing portion are shown with a marker between them counting the surviving lines that were left out. Every gap between non-adjacent surviving lines is marked with a count of what was skipped, so the reader can always see that something is missing and how much. This applies only to output the wrapper is reducing — an unwrapped command is never shortened (8.7). | |

## 9. Design Invariants

**The `cant-break-by-design` skill is mandatory** (9.1): it is invoked for every
design decision and every code path, in any language. 85 of the 185 rows point
at it — a pointer being a universal cell that BEGINS with
`→ cant-break-by-design`, not merely one that names the skill. In 79 of them
the pointer is the whole cell, because the skill already
makes that demand in full and the union will not restate it; in the other six
(9.13, 9.35, 9.85, 9.105, 9.106, 9.111) a `, plus:` clause follows, holding the
one thing the row adds to the section it points at. Either way the pointer
names the section
that governs, and the two project columns stay as the record of where the
demand reached us from. The remaining 99 rows extend the skill outright (of
which 7 are excluded by owner ruling): each adds a demand, a mechanism or a
scope it does not state. Extensions, never substitutes.

Owner ruling, 2026-09-01: rows 9.165–9.171 (the persisted-layouts block) are
marked `✗ not in union`; phase 3 does not carry them.

*The largest group, and the one whose rules are most entangled with what each
project builds — so every universal cell states the underlying engineering
principle rather than the domain it was learned in. Siblings that fire on other
triggers stay where their trigger lives: a check that cannot run failing loudly
is 8.31, a gate printing its own count is 8.21, proving a change against a
stand-in for the real thing is group 7, the poisoned-build-cache symptom is
5.16, and reading someone else's implementation is group 10. Rows 9.13–9.22 are
the shared strength scale the rest of the group refers to; 9.49–9.75 are the
catalog of ways a claim outruns its mechanism, each one a distinct thing an
audit must catch — both of those runs now point at the skill that publishes
them rather than repeating it.*

| id | ferrislicer | dwc-ng | proposed universal form |                                                      |
|---|---|---|---|------------------------------------------------------|
| 9.1 | § Design (Code): enforce cant-break-by-design for every decision; the register's master family — bad states made structurally impossible, never merely prevented by a rule embedded in the logic — and every other hard rule is an instance of it | States it outright in its working rules: "nothing should be able to break by construction" (`CLAUDE.md` § Working rules (design) — restored in Task 8 from the deleted domain section that had misfiled it; see that project's NOTES.md). Its persisted-state spec is the same demand applied: "what must be unrepresentable", coupled "by a mechanism, not a prose must" | The `cant-break-by-design` skill is MANDATORY: it is invoked for every design decision and every code path, in any language, not consulted when someone remembers it. Its rule is the one every other hard rule here is an instance of — every design decision makes the bad state structurally impossible rather than forbidden by a rule someone has to remember, by a mechanism and never by a prose "must". The design rules in this group are read as extensions of that skill, never as substitutes for it: where a row points at a section of the skill, that section is the text that governs. | this is referring to the /cant-break-by-design skill |
| 9.2 | Name the invariant in one sentence before writing the code path; if you cannot state it, the design is not ready | — | → cant-break-by-design § 4. The process — name the invariant before writing the path |                                                      |
| 9.3 | A feature enumerates invariants in BOTH directions: existing ones it touches and new ones its own output introduces; an unnamed invariant ships at rung 0 | — | → cant-break-by-design § 4. The process — both directions: invariants touched and invariants introduced |                                                      |
| 9.4 | Pick the highest enforcement rung the language allows; 7–8 are the target, 5–6 the weakest acceptable interim and only with a ledger row naming the promotion | — | → cant-break-by-design § 1. The ladder |                                                      |
| 9.5 | A shared closure or helper ("call this") is the weakest acceptable form and carries a promotion debt | — | → cant-break-by-design § 4. The process — shared helper is the weakest acceptable form, carries promotion debt |                                                      |
| 9.6 | Where the language cannot enforce an invariant structurally, move it into a stronger layer — types, then codegen, then build-time checks, then a sealed module; the requirement never changes, only the technique | — | → cant-break-by-design § 3. Strongest tool per language — types, then codegen, then build-time checks, then a sealed module |                                                      |
| 9.7 | Shrink the trusted core — parser, unsafe block, foreign-function edge — to be small, sealed, and the only place the invariant is even expressible; back it with lints and generate the rest | — | → cant-break-by-design § 2. Techniques — shrink the trusted core |                                                      |
| 9.8 | The tripwire: duplicating a required processing step at a SECOND call site means the design is already wrong — stop and build the choke-point instead of pasting | — | → cant-break-by-design § 4. The process — the tripwire |                                                      |
| 9.9 | The stranger test: would a new call site added by someone who read no documentation still be correct? If not, keep climbing the ladder | — | → cant-break-by-design § 4. The process — the stranger test |                                                      |
| 9.10 | Assign the rung from the mechanism that actually enforces it, never from how confident the prose sounds; a claim naming no mechanism is rung 0 | — | → cant-break-by-design § 4. The process — assign the rung from the mechanism, never the wording |                                                      |
| 9.11 | An enforcement rung measures only bypass-resistance; whether the stated claim is the CORRECT claim is checked separately | — | The strength rating measures only how hard the rule is to bypass. Whether the rule is the right rule is a separate question, checked separately. |                                                      |
| 9.12 | A consuming handoff that makes stale data fail to compile outranks an accessor that re-checks and detects staleness — unrepresentable outranks detected | — | → cant-break-by-design § 1. The ladder — unrepresentable outranks detected |                                                      |
| 9.13 | Enforcement strength is a published 8-rung ladder in `docs/INVARIANTS.md`, reproduced in the auditing agent so it never audits without it | — | → cant-break-by-design § 1. The ladder, plus: any tool that judges enforcement strength carries the published scale with it, so it never judges without it. |                                                      |
| 9.14 | Rung 0 — comment or documentation: not enforcement | — | → cant-break-by-design § 1. The ladder — rung 0, comment or documentation |                                                      |
| 9.15 | Rung 1 — convention and review: not enforcement | — | → cant-break-by-design § 1. The ladder — rung 1, convention and review |                                                      |
| 9.16 | Rung 2 — runtime assert: detection, not prevention | — | → cant-break-by-design § 1. The ladder — rung 2, runtime assert |                                                      |
| 9.17 | Rung 3 — tests: pin known cases only | — | → cant-break-by-design § 1. The ladder — rung 3, tests |                                                      |
| 9.18 | Rung 4 — lint or static analysis: a fence, not a wall | — | → cant-break-by-design § 1. The ladder — rung 4, lint or static analysis |                                                      |
| 9.19 | Rung 5 — shared helper: single implementation, optional use | — | → cant-break-by-design § 1. The ladder — rung 5, shared helper |                                                      |
| 9.20 | Rung 6 — choke-point, the sole visible route: strong, still structural discipline | — | → cant-break-by-design § 1. The ladder — rung 6, choke-point |                                                      |
| 9.21 | Rung 7 — sole-constructor type, bypass is a compile error: by design | — | → cant-break-by-design § 1. The ladder — rung 7, sole-constructor type |                                                      |
| 9.22 | Rung 8 — illegal state unrepresentable: by design, nothing left to break | — | → cant-break-by-design § 1. The ladder — rung 8, illegal state unrepresentable |                                                      |
| 9.23 | Model data so invalid combinations have no encoding: flag clusters become enums, empty-able lists become non-empty types, parallel arrays become objects holding direct references | — | → cant-break-by-design § 2. Techniques — illegal states unrepresentable |                                                      |
| 9.24 | Parse, don't validate: validation returns a NEW type carrying the proof, the check happens once at the boundary, and the unchecked value must not exist downstream | — | → cant-break-by-design § 2. Techniques — parse, don't validate |                                                      |
| 9.25 | Untrusted external text becomes typed through exactly ONE boundary; no second parser or typer is written | — | → cant-break-by-design § 2. Techniques — parse, don't validate; one boundary, no second parser |                                                      |
| 9.26 | The only way to obtain a value is the sole constructor that establishes its invariant, with private fields and a private constructor | — | → cant-break-by-design § 2. Techniques — smart constructors |                                                      |
| 9.27 | Sealed pipeline: where an invariant requires a fixed sequence of steps, the sink accepts ONLY the pipeline's output type, so the parameter type is the enforcement | — | → cant-break-by-design § 2. Techniques — sealed pipelines and sinks |                                                      |
| 9.28 | Capability token: an operation requiring authority takes a token type only the authority grants, so the permission check cannot be forgotten | — | → cant-break-by-design § 2. Techniques — capability objects |                                                      |
| 9.29 | Typestate: encode the state machine in the type system so misuse such as send-before-open or double-close is a compile error | — | → cant-break-by-design § 2. Techniques — typestate |                                                      |
| 9.30 | Domain sums have NO default match arms, so adding a case is a compile error at every site; prefer total functions over partial ones guarded by callers | — | → cant-break-by-design § 2. Techniques — totality and exhaustiveness, no default arms |                                                      |
| 9.31 | Every new case lands together with its handling in the same change, so the set stays exhaustive and every intermediate state compiles | — | → cant-break-by-design § 2. Techniques — exhaustiveness: a new case lands with its handling |                                                      |
| 9.32 | Never pass raw numbers across a boundary where the unit or frame matters; distinct types per unit and domain make mixing them a type error | — | → cant-break-by-design § 2. Techniques — units as types |                                                      |
| 9.33 | A newtype is only as strong as where it is MINTED: hand-wrapping at a call site lets two adjacent wraps be transposed and catches nothing — mint at the authority that reads the source of truth, so key, direction and return type are declared together | — | A distinguishing type is only as strong as the place it is created. Wrapped by hand at each call site, two adjacent wrappings can be swapped and the type catches nothing. Create it at the authority that reads the source of truth, so the source, the direction and the type are declared in one place. |                                                      |
| 9.34 | When the minter and the consumer are modules that do not depend on each other, the type lives in the zero-dependency vocabulary module — promoting the consumer into the minter's dependencies pays for the invariant with whatever property that module was built for | — | When the place that creates such a type and the place that consumes it do not depend on each other, the type goes in a shared vocabulary unit with no dependencies of its own. Making one depend on the other pays for the invariant with whatever property that unit was built to have. |                                                      |
| 9.35 | The wrapper's own fields go private in the SAME change, with compile-fail proofs for the field read, the struct literal and the cross-assignment | — | → cant-break-by-design § 5. Anti-patterns — A5.1, plus: with proofs that the field read, the direct build and the cross-assignment each fail to compile. |                                                      |
| 9.36 | A function requiring a prepared input takes ONLY the type whose sole constructor performs that preparation, never the raw underlying type, so skipping it is a compile error | — | → cant-break-by-design § 2. Techniques — sealed pipelines and sinks |                                                      |
| 9.37 | Enforcement is the TYPE, not a review rule: the type is constructed only by its owning module, and consumers must name the grade or variant they are entitled to as a parameter | — | → cant-break-by-design § 2. Techniques — smart constructors and capability objects |                                                      |
| 9.38 | A composite with multiple derived substructures gets a single sole constructor that builds them all in one pass, so they can never be born desynchronized | — | → cant-break-by-design § 2. Techniques — derive, don't duplicate; one constructor builds every part |                                                      |
| 9.39 | A flat-to-kind partition exists in exactly ONE place, as a sole-constructor type with an exhaustive routing match and no default arm; never add a second classification loop | — | → cant-break-by-design § 2. Techniques — one exhaustive classifier; a second one is the tripwire |                                                      |
| 9.40 | Derive, don't duplicate: a value is stored once and everything else computed from it at use time or by a build step | — | → cant-break-by-design § 2. Techniques — derive, don't duplicate |                                                      |
| 9.41 | N artifacts that must agree — schema, parser, docs, UI, migrations — are ONE artifact plus a generator, never hand-synchronized | — | → cant-break-by-design § 2. Techniques — generate, don't hand-maintain |                                                      |
| 9.42 | Compile in runtime-loadable definitions rather than leaving them loadable | — | Definitions that could be loaded at run time are compiled in instead, so there is no path by which a different set arrives later. |                                                      |
| 9.43 | Tie resource lifetime to value lifetime via ownership, so leaks, double-frees and use-after-close are unrepresentable or compile errors | — | → cant-break-by-design § 2. Techniques — RAII and linearity |                                                      |
| 9.44 | Shared-mutable state is the state to make unrepresentable, not the default to discipline: immutability by default, mutation a marked, scoped exception | — | → cant-break-by-design § 2. Techniques — immutability by default |                                                      |
| 9.45 | Make concurrency hazards unrepresentable: data races via ownership transfer, actors or immutability; orphaned tasks via structured concurrency; deadlocks via lock ordering encoded as types | — | → cant-break-by-design § 2. Techniques — concurrency by construction |                                                      |
| 9.46 | Where retries, reorderings or merges are possible, design operations so applying them twice, out of order, or concurrently is defined and identical, so the conflict cannot occur | — | → cant-break-by-design § 2. Techniques — idempotence, commutativity, monotonicity |                                                      |
| 9.47 | Existing bank and map objects are consumed read-only rather than building new index tables beside them | — | → cant-break-by-design § 2. Techniques — derive, don't duplicate; no second index beside the first |                                                      |
| 9.48 | Shared per-layer geometry is borrowed, not cloned: a sole-constructor type with private fields and slice-returning accessors makes the ownership design and the safety invariant the same design | — | → cant-break-by-design § 2. Techniques — smart constructors; borrow rather than copy |                                                      |
| 9.49 | A stated invariant's enforcement strength must MATCH its actual mechanism; the anti-pattern catalog below is the standing list of shapes where the stated strength exceeds the real one, and every one of them must always be flagged | — | → cant-break-by-design § 5. Anti-patterns |                                                      |
| 9.50 | `pub` fields (or a `pub` tuple field) under a sole-constructor claim: the fields become private in the same commit, or the claim is not written | — | → cant-break-by-design § 5. Anti-patterns — A5.1, a sole-constructor claim needs private fields |                                                      |
| 9.51 | Private fields are a MODULE wall, not a type wall: a sole-constructor claim on a type declared in a large file is rung 6, because every line of that file can still write the literal; fix by moving it into its own module, and keep it fixed with a source-scanning guard, because moving it back out compiles perfectly | — | Field privacy is a wall around the enclosing unit, not around the type: while the type is declared inside a large file, everything in that file can still build one directly. Move it into its own unit, and keep it there with a check over the source, because moving it back out compiles perfectly and no compiler will report it. |                                                      |
| 9.52 | A `derive` is a public constructor and the quieter door: a derived empty value mints one inland and a derived deserializer sets every private field from untrusted text, yet nothing in the struct body looks wrong; fix with a private zero-sized witness field whose type implements neither, plus a source-scanning guard, because deleting the one-line witness compiles | — | An automatically generated constructor is a public constructor, and the quieter one: a generated empty value builds one from anywhere, and a generated decoder fills every private field from untrusted text — while nothing in the declaration looks wrong to a reader checking field visibility. Block both with a private marker member whose own type supports neither, and keep a source check, because deleting that one line compiles. |                                                      |
| 9.53 | A runtime assertion described as though it prevented the state: it is a rung-2 guard in one build profile and nothing in the shipped one, so it is promoted or declared in the build-profile register with its release behaviour spelled out | — | → cant-break-by-design § 5. Anti-patterns — A5.12, a debug-only assertion is not enforcement |                                                      |
| 9.54 | Every profile-dependent guard declares the release-build behaviour it degrades to | — | → cant-break-by-design § 4. The process — declare the release behaviour of every profile-dependent guard |                                                      |
| 9.55 | Prefer identical debug and release behaviour over a debug-only guard: a bug reproducible in only one profile has to be debugged twice, the second time in the field | — | → cant-break-by-design § 5. Anti-patterns — A5.14, prefer identical debug and release behaviour |                                                      |
| 9.56 | A fault-isolation boundary states which build profile it actually protects and names what defends the other, since an abort-on-panic setting makes it inert | — | → cant-break-by-design § 5. Anti-patterns — A5.13, fault isolation states the profile it works in |                                                      |
| 9.57 | A caller precondition stated in prose must be encoded as a type; if a type is genuinely not worth minting, the prose says "precondition, unchecked" so it can be tracked | — | → cant-break-by-design § 5. Anti-patterns — A5.5, a caller precondition is a type, not a sentence |                                                      |
| 9.58 | A module stating "enforcement is the caller's job" has no invariant of its own: either accept a type buildable only from conforming input, or record the invariant against the caller, never the module | — | → cant-break-by-design § 5. Anti-patterns — A5.6, a unit disclaiming enforcement has no invariant |                                                      |
| 9.59 | A type name or doc-comment warning is not prevention: if two meanings of a value are both legitimate, they must be two types, never one name with a caveat | — | → cant-break-by-design § 5. Anti-patterns — A5.2, naming is not prevention |                                                      |
| 9.60 | Co-location of two pieces of data in the same file is not a mechanism: either one derives from the other, or they are a single value | — | → cant-break-by-design § 5. Anti-patterns — A5.3, co-location is not a mechanism |                                                      |
| 9.61 | "These two cannot disagree, because one is built from the other" is a claim about the PRODUCER and says nothing about the value once it exists: a stored re-derivation is still a re-derivation — store each fact once and compose on demand | — | → cant-break-by-design § 2. Techniques — derive, don't duplicate; a stored re-derivation is still stored twice |                                                      |
| 9.62 | A bound such as a numeric limit exists in exactly one place: stated twice, the two copies will disagree, so one derives from the other or both import a shared constant | — | → cant-break-by-design § 5. Anti-patterns — A5.7, a bound may exist in exactly one place |                                                      |
| 9.63 | A hand-maintained mirror of another module's data needs a GENERATOR, not a drift test: generate the list, invert the dependency, or move the consumer | — | → cant-break-by-design § 5. Anti-patterns — A5.8, a mirror needs a generator, not a drift test |                                                      |
| 9.64 | Duplicating the same clamp or bound check in every setter is the tripwire: use a single constructor or a range newtype instead | — | → cant-break-by-design § 5. Anti-patterns — A5.9, clamping in every setter is the tripwire |                                                      |
| 9.65 | A hand-maintained exception or allowlist is debt and must be filed as debt with a ledger row naming the eventual promotion; it is not optional | — | → cant-break-by-design § 5. Anti-patterns — A5.10, a hand-maintained exception list is filed debt |                                                      |
| 9.66 | "By construction, pinned by a test" is only rung 3: the prose says "enforced by test", and "by construction" is reserved for mechanisms that make the violation unwritable | — | → cant-break-by-design § 5. Anti-patterns — A5.11, "pinned by a test" is rung 3 |                                                      |
| 9.67 | "Cannot fail in practice" plus an unwrapping call is a panic: either prove the impossibility from the types, or make the function return a fallible result | — | → cant-break-by-design § 5. Anti-patterns — A5.15, "cannot fail in practice" plus an unwrap is a panic |                                                      |
| 9.68 | A silent bounded degrade, such as a search cap, publishes its MEASURED divergence point — the actual input size where the cap starts changing the result — not just the bound itself | — | → cant-break-by-design § 5. Anti-patterns — A5.16, a bounded degrade publishes its divergence point |                                                      |
| 9.69 | An invariant attached to code that cannot actually run says so explicitly: an unlabelled dead guard reads as live protection and will be trusted by the next person who touches it | — | → cant-break-by-design § 5. Anti-patterns — A5.17, an invariant on code that cannot run says so |                                                      |
| 9.70 | When one implementation supersedes another, delete the superseded alternative rather than leaving it unused: an unused function is an invitation for the next caller to pick it | — | → cant-break-by-design § 5. Anti-patterns — A5.18, delete the alternative, don't leave it unused |                                                      |
| 9.71 | A run-scoped mutable global is only rung-2 enforcement wherever tests can share a process: thread the value explicitly or hand out a handle that states it, and treat an unavoidable global as debt on arrival | — | → cant-break-by-design § 5. Anti-patterns — A5.19, a run-global is rung 2 wherever tests share a process |                                                      |
| 9.72 | A role, flag or enum variant used as a TRANSPORT for information that wants a description channel: before a role is removed or added, ask what it was carrying | — | A category, flag or case used to smuggle information that really wants a channel of its own. Before adding or removing one, ask what it was actually carrying. |                                                      |
| 9.73 | A test standing in for construction is legitimate but pins known cases only; if the doc says "by construction" and the mechanism is a test, the doc is wrong | — | → cant-break-by-design § 5. Anti-patterns — A5.11, tests standing in for construction |                                                      |
| 9.74 | Certain doc-comment phrases — "by convention", "callers must", "should", "in practice", "not yet", "deferred", "hand-maintained", "pinned by a test" — mark a claim weaker than it sounds and oblige either a promotion or a ledger row in the same commit | — | → cant-break-by-design § Red-flag phrases |                                                      |
| 9.75 | "Should" in a doc comment is not a rule: make the guarded thing the only reachable currency, or explicitly accept rung 1 and file a ledger row for it | — | → cant-break-by-design § 5. Anti-patterns — A5.4, "should" is not a rule |                                                      |
| 9.76 | An invariant and its ledger row land in the SAME commit; an invariant that exists only in a doc comment is not enumerable | — | → cant-break-by-design § 4. The process — state the invariant and file its row in the same commit |                                                      |
| 9.77 | Every new code path gets an invariant test proving the invariant holds through it, plus a red-check proving that test can fail; the type enforces, the test only pins | — | → cant-break-by-design § 4. The process — new path ⇒ invariant test, plus a red-check |                                                      |
| 9.78 | Grandfathering: touching code near a rung-5-or-lower invariant obligates either promoting it or adding a ledger entry describing the deferral — debt is allowed, silent debt is not | — | → cant-break-by-design § 4. The process — grandfathering; debt is allowed, silent debt is not |                                                      |
| 9.79 | A ledger row anchors on a SYMBOL, never on a line number: a moved line fails silently, a renamed symbol fails loudly; where a claim has no symbol, name the enclosing one or quote the line verbatim — refused mechanically, which supersedes the older rule of flagging stale citations | — | An entry in the ledger anchors on a named thing, never on a line number: a line that moves fails silently, a name that changes fails loudly. Where a claim has no name of its own, name the thing enclosing it or quote the line word for word. The wrong form is refused mechanically, so there is nothing left to flag by eye. |                                                      |
| 9.80 | A diff that deletes prose an invariant ledger cites is itself a FINDING that must get a row — either a promotion or an abandonment — never silence | — | → cant-break-by-design § 6. Continuous review — a deleted claim is a finding |                                                      |
| 9.81 | Continuous ledger review is OFFERED, never assumed: ask whether a background agent should keep the ledger current, and accept "no" as an answer | — | → cant-break-by-design § 4. The process — offer continuous review; do not assume it |                                                      |
| 9.82 | The ledger goes stale on the next commit that adds a guard, mirrors a list, or writes "callers must", so keeping it current is mechanical work that must not run without consent, because it costs real tokens | — | → cant-break-by-design § 6. Continuous review — the ledger goes stale on the next commit |                                                      |
| 9.83 | A continuous ledger reviewer's scope is limited to recently changed code — the diff since the last ledger update — and this is a fixed rule, not a tunable default | — | → cant-break-by-design § 6. Continuous review — scope is recently changed code |                                                      |
| 9.84 | It triggers on demand or after a merge, never on every commit | — | It runs on request or after a merge, never on every commit. |                                                      |
| 9.85 | It outputs only proposed rows, downgrades and fixes — never an unattended source edit — and writes only to the ledger, on a clean run | — | → cant-break-by-design § 6. Continuous review, plus: it writes only the ledger, and only when its run was clean. |                                                      |
| 9.86 | It escalates any rung-dropping change IMMEDIATELY rather than batching it | — | → cant-break-by-design § 6. Continuous review — escalate a dropped rung immediately |                                                      |
| 9.87 | It is read-only over source: it proposes changes, it never promotes or "fixes" an invariant itself | — | → cant-break-by-design § 6. Continuous review — read-only over source; it proposes, it does not promote |                                                      |
| 9.88 | It reports the MECHANISM behind a rung assignment, not just the rung number, since the rung is a judgement call that must be reviewable | — | → cant-break-by-design § 6. Continuous review — report the mechanism, not the verdict |                                                      |
| 9.89 | Never re-derive: a fact is computed ONCE, at its authority, and every consumer READS it — no stage, emitter, parser or probe recomputes a private opinion of an existing fact, because a second derivation WILL disagree and the disagreement wears a defect's face | — | A fact is computed once, at the place that owns it, and everything else reads it. Nothing downstream recomputes its own opinion of a fact that already exists, because a second derivation will eventually disagree with the first, and that disagreement arrives looking exactly like a defect. |                                                      |
| 9.90 | Formats: the producer states the fact explicitly; an encoding a consumer must infer from arithmetic is a re-derivation forced on every reader forever — the format is wrong | — | In a stored or transmitted format, the producer states the fact outright. If a consumer has to infer it by arithmetic, the format is wrong: it has forced the same re-derivation on every reader, forever. |                                                      |
| 9.91 | Probes dump the binding the pipeline ACTUALLY uses, never a recomputed "equivalent": three attempts were judged against a set no stage ever saw | — | A diagnostic dump shows the values the system actually used, never a recomputed equivalent. Otherwise the work is judged against something the system never saw. |                                                      |
| 9.92 | Generation-time classification is authoritative and annotations flow from it; consumers never re-classify | — | A classification made where the data is produced is authoritative, and everything downstream carries it forward. Consumers never classify the same thing again for themselves. |                                                      |
| 9.93 | The rule governs the PRODUCTION path and INVERTS in a test's expected value: inside the pipeline a second opinion is a defect, on the right-hand side of an assertion it is the entire point — an expectation taken from the engine can only report that it changed, never that it is wrong | — | This rule governs the working path and inverts inside a test's expected value: in the system a second opinion is a defect, but on the other side of an assertion it is the whole point. An expectation taken from the thing under test can only report that it changed, never that it is wrong. |                                                      |
| 9.94 | MEASUREMENT comes from the engine; EXPECTATION comes from a non-engine source — the config the axis states, the dimension the fixture states, the input's own cross section, or arithmetic. Both sides from the engine is self-consistency, and a uniform error passes it | — | Take the measurement from the system under test, and the expectation from something the system never produced: the stated setting, the fixture's own dimensions, the input's own shape, or arithmetic. With both sides from the system you have only proved it agrees with itself, and an error affecting everything passes. |                                                      |
| 9.95 | A number pasted from a probe run is a REGRESSION PIN, not a test: legitimate and useful on a deterministic engine, but labelled as a pin and never counted as coverage | — | A number copied out of a diagnostic run is a pin against regression, not a test. On a deterministic system it is useful — every change to it is signal — but it is labelled as a pin and never counted as coverage. |                                                      |
| 9.96 | Where an expectation genuinely cannot be derived yet, say so AT THE ASSERTION SITE and leave the work undone rather than pinning the engine's current answer and calling it a contract | — | Where an expectation genuinely cannot be derived yet, say so at the assertion itself and leave the work undone, rather than pinning today's answer and calling it a contract. |                                                      |
| 9.97 | The process environment is a FACT: every `FS_*` switch is resolved in ONE `traps` module per crate and everything inland reads a field; the name appearing anywhere else in that crate is a bug. "Call frequency doesn't matter, it's a matter of design" | — | The process environment is a fact like any other: every switch is resolved once, at one authority per unit, and everything inside reads a field. The switch's name appearing anywhere else in that unit is a defect. How often it is read is not the criterion; where it is read is. |                                                      |
| 9.98 | Mechanically checked, not promised: each authority ships a test grepping its own crate for environment reads and failing on any site but itself — a doc comment stops nobody | — | The single authority is checked mechanically: it ships a test that scans its own unit for environment reads and fails on any site but itself. A comment asking people not to stops nobody. |                                                      |
| 9.99 | A field read is not the whole fix: a switch gating a PER-ELEMENT decision is chosen once outside the loop and monomorphised so the branch compiles out; a cached read still costs a branch per element and was explicitly rejected as sufficient | — | Reading a field is not always the whole fix: where a switch decides something per item, choose the path once outside the loop so the branch disappears entirely. A cached read still costs a test per item, and that was explicitly rejected as good enough. |                                                      |
| 9.100 | Cost is the symptom, not the rule: one switch was read once per segment against 1.6 M segments, but the rule would hold at one call per slice — scattered authorities are the defect | — | Cost is how this gets noticed, not what the rule is about. Scattered authorities are the defect, and the rule holds just as firmly when the wasteful reading is a single call per run. |                                                      |
| 9.101 | Per-crate is where a switch STARTS, not a licence to claim a name another crate already claims: a switch two crates honour is resolved in the crate that owns the FACT it arms, and the others READ the resolved value as data — reading is not re-deriving, parsing the name a second time is | — | One authority per unit is where a switch starts, not permission to claim a name another unit already owns. A switch two units honour is resolved in the one that owns the fact it arms, and the others read the resolved value as data. Reading it is not re-deriving; parsing the name a second time is. |                                                      |
| 9.102 | Each crate's own single-reader test cannot see this, so the NAME is gated workspace-wide: spelled ONCE as a `const` the hint tables read, with a test walking every crate's source and failing on a second spelling | — | A per-unit check cannot see a name duplicated across units, so a shared name is spelled once as one shared definition, and a project-wide check walks every unit and fails on a second spelling. |                                                      |
| 9.103 | A table that MIRRORS where something is emitted is checked mechanically or it is not a table, it is a claim: a stale hint tells an operator to set a directive that never fires — one shared gate per crate PLUS a workspace sweep, so a crate that grows a table and never opts in is still checked | — | A table that mirrors where something is emitted is checked mechanically, or it is not a table but a claim: gone stale, it tells an operator to turn on something that will never fire — the exact silence it existed to prevent. One shared check per unit, plus a project-wide sweep, so a unit that grows such a table and never opts in is still covered. |                                                      |
| 9.104 | A grep gate always ships its positive control: one that stops matching reads exactly like a conforming crate | — | A check built on searching always ships a case proving it still matches. One that has quietly stopped matching looks exactly like a codebase that complies. |                                                      |
| 9.105 | A number means that number: never `-1`, `0` or `9999` standing for unset, auto, inherit or disabled, and never `""` except where it is a list-shaped key's own declared empty collection (earlier form, 2026-08-02: `""` banned outright; narrowed 2026-08-27, see 9.108) | — | → cant-break-by-design § 2. Techniques — illegal states unrepresentable; no sentinel values, plus: the ban covers a legal value made to stand for a different concept, never a collection's own emptiness (9.108). |                                                      |
| 9.106 | Absence is an absent key or an optional type — an optional ONLY where absence is the signal that a value is inherited from elsewhere; auto is an enum variant or its own bool, and numeric fields hold numbers (earlier form, 2026-08-02: absence is an optional type or an absent key, unqualified; narrowed 2026-08-27 into the three classes at 9.113–9.115, where a directly-set value returns a plain value and never an optional) | — | → cant-break-by-design § 2. Techniques — illegal states unrepresentable; absence is an optional type, plus: which of the two shapes absence takes is fixed by the setting's class, not by the author's taste — an optional is reserved for the case where absence is the signal (9.113–9.115). |                                                      |
| 9.107 | An upstream format forcing a sentinel is converted at the boundary, ONCE, and never let inland | — | → cant-break-by-design § 2. Techniques — parse, don't validate; convert at the boundary, once |                                                      |
| 9.108 | An empty-collection default is NOT a sentinel: `-1` for "inherit the fill angle" smuggles a different concept inside a legal value, while `default = ""` on a list-shaped key decoding to zero elements IS the value. One question tells them apart: does absence mean inherit ANOTHER key's value, or zero elements of THIS key's own collection? | — | An empty collection used as a default is not a stand-in value: a magic number meaning "inherit" smuggles a different concept inside a legal value, whereas an empty list decoding to zero elements simply is the value. One question separates them: does absence mean inherit another setting's value, or zero elements of this setting's own collection? |                                                      |
| 9.109 | Inherits another key → declare NO default, ever: a fabricated default on an override key silently severs inheritance, the exact damage this rule exists to stop | — | If absence means inherit another setting's value, declare no default at all, ever. A fabricated default there silently severs the inheritance, which is the exact damage this rule exists to prevent. |                                                      |
| 9.110 | Zero elements of its own collection → declare the empty value AS the default, matching the reference format's own empty defaults; withholding it from a UI-exposed, non-override key panics the registry BUILD | — | If absence means zero elements of that setting's own collection, declare the empty value as its default, matching the reference format's own empty defaults. Withholding it makes the definition table fail to build. |                                                      |
| 9.111 | "Flag anything that can panic on absence — literally anything. That's cant-break-by-design" | — | → cant-break-by-design § 5. Anti-patterns — A5.15, plus: the sweep is unbounded — anything at all that can fail catastrophically on absence gets flagged, not only the sites someone suspects. |                                                      |
| 9.112 | NOT "give everything a default": filling one in for every key is a regression, not a fix, because it silently severs the keys whose absence means "inherit the base value" | — | This is not "give everything a default". Filling one in everywhere is a regression, not a fix, because it silently severs every setting whose absence meant inherit. |                                                      |
| 9.113 | Class one, a directly-set value: must declare a default, the accessor returns a plain value, and absence must be impossible BY CONSTRUCTION — not merely believed because some layout row happens to exist today | — | First class, a value someone sets directly: it declares a default, its accessor returns a plain value, and its absence is impossible by construction — not merely believed, because some other table happens to carry a matching row today. |                                                      |
| 9.114 | Class two, computed by a registered relation and never authored directly: no static default is meaningful, and the accessor must never fall through to a raw registry read of a key never meant to carry one | — | Second class, a value computed from others and never authored directly: no fixed default means anything for it, and its accessor never falls back to a raw lookup of something that was never meant to hold a stored value. |                                                      |
| 9.115 | Class three, an override or optional — also free text, command actions, templates: no default, the accessor returns an optional, the caller decides; absence IS the signal, not a gap | — | Third class, an optional override — also free text, actions and templates: no default, the accessor returns an optional value, and the caller decides. Here absence is the signal, not a gap. |                                                      |
| 9.116 | In every class a panic is the same defect: a return type that claims "always present" without a mechanism that makes that true | — | → cant-break-by-design § 5. Anti-patterns — A5.15; a return type claiming presence needs a mechanism |                                                      |
| 9.117 | REFINES the no-defaults rule, does not supersede it: inventing a value for a key that should have one stays forbidden. This rule's target is presence merely BELIEVED — contingent on a row nobody re-derives against — rather than enforced at build time or in the return type | — | This sharpens the rule against invented defaults rather than replacing it: inventing a value for a setting that should have one stays forbidden. What it targets is presence merely believed — resting on some other artifact nobody re-checks — rather than enforced when the thing is built or by the return type itself. |                                                      |
| 9.118 | External input NEVER panics: a malformed or truncated file, a hand-edited preset, a foreign product's config, a bad command-line flag is DATA, not our own invariant failing — it produces a diagnostic the user sees, never a crash | — | External input never crashes the product. A malformed or truncated file, a hand-edited settings file, another product's configuration, a bad command-line flag: all of these are data, not your own invariant failing, and they produce a diagnostic the user sees. |                                                      |
| 9.119 | The boundary resolves absence into a value, a typed absence, or a reported rejection, so inland code needs no panic guard — the unrepresentable state cannot reach it | — | → cant-break-by-design § 2. Techniques — parse, don't validate; the boundary resolves absence |                                                      |
| 9.120 | Claimed ground is excised ground: a step that cookie-cutters geometry PERMANENTLY excises it from the layer — once a cutter claims ground, no later step may receive it; the same geometry reaching two excising steps is a bug class found repeatedly by exactly this logic | — | Once a stage consumes a resource, that resource is permanently removed from what any later stage can receive. The same resource reaching two consuming stages is a defect class that keeps recurring, and this is the rule that finds it. |                                                      |
| 9.121 | The cut excises BEFORE any downstream ground is derived; ordering is part of the rule | — | The removal happens before anything downstream is derived from what remains. The ordering is part of the rule, not an implementation detail. |                                                      |
| 9.122 | RETRACTION vs RESIDUE: two different decline events that must not share a word | — | The two ways a stage can decline a resource are different events and never share a word: giving back a claim it never exercised, and passing on what it could not use of a claim it did. |                                                      |
| 9.123 | A cutter that decides NOT to take a candidate cut — nothing deposited, nothing derived downstream yet — RETRACTS: the ground rejoins THE ORIGINAL OWNER as if never cut, at most once per site, and flows the normal partition. The pipeline is the router; the origin holds no routing knowledge | — | A stage that declines a claim it never exercised — nothing produced, nothing yet derived downstream — returns the resource to its original owner as if it had never been taken, at most once per site, after which it flows through the normal division again. The pipeline is the router; the origin holds no routing knowledge of its own. |                                                      |
| 9.124 | Ground that fell out of an EXERCISED claim is RESIDUE: never backward, forward to the ONE successor the design names at that boundary, witnessed — and the chain terminates in a deposit or a declared, warned scrap | — | What is left over from a claim the stage did exercise never travels backward. It goes forward to the one successor the design names at that boundary, recorded as it goes, and the chain ends either in use or in a declared, warned discard. |                                                      |
| 9.125 | Culled ground with no recipient is a void; ground re-reachable by two recipients is a double-write | — | Every declined resource has exactly one named recipient. None is a silent void; two is a double write. |                                                      |
| 9.126 | Ownership of DEPOSIT ground, not single-touch: ground may be PROCESSED more than once — ironing over a top skin is fine | — | The rule is about who consumes a resource, not how many times it may be touched. Re-processing something a stage already consumed is fine. |                                                      |
| 9.127 | The subtraction that excises uses the set the cutter ACTUALLY deposited or formally claimed, never a recomputed equivalent — probes and remainders alike observe the real binding | — | What gets removed is exactly what the stage actually produced, or formally claimed, never a recomputed equivalent. Diagnostics and remainders alike observe the real thing. |                                                      |
| 9.128 | Audit question number one is "which two steps both received this ground?" — a static lookup, because every decline site's recipient is named in the design; the census only sees the collision, not the submission | — | The first question in an audit is which two stages both received the same resource. It has a lookup answer, because every decline site's recipient is named in the design — and a census can only ever see the collision, never the handover that caused it. |                                                      |
| 9.129 | Warn loudly: anything the user gave us that we drop, ignore, skip, clamp, substitute or fail to import, SAY SO where they will see it. Silence reads as success | — | Anything the user gave you that gets dropped, ignored, skipped, clamped, substituted or never read is said where they will see it. Silence reads as success. |                                                      |
| 9.130 | "We never tried" counts: opening a foreign project file takes the geometry and discards the entire print configuration silently — "no importer yet" is a warning, not an exemption | — | Never having tried counts. Taking part of what a file contains and quietly discarding the rest still needs saying: having no reader for something yet is a warning, not an exemption from one. |                                                      |
| 9.131 | Warn, do not fail: loading succeeds and the user judges the degraded result — they just cannot judge it blind | — | Warn, do not fail. The operation still succeeds and the user judges the degraded result for themselves; they simply cannot judge it blind. |                                                      |
| 9.132 | The bar is THEIR expectation, not our contract: clamping inside the declared range is still not the number they typed | — | The bar is the user's expectation, not your documented contract. A value clamped to something inside the allowed range is still not the number they typed. |                                                      |
| 9.133 | Model to copy: the config import report lists unknown keys, untranslatable keys and clamps, per preset, plus a permanent "REVIEW BEFORE PRINTING: custom content is carried verbatim, not interpreted" | — | The model to copy is a per-source report listing what was not recognised, what could not be translated and what was clamped — plus a standing notice for anything carried through word for word and never interpreted, because the product cannot vouch for it. |                                                      |
| 9.134 | Config fields stop at the hub: a field is implemented when storage, defaults and hub wiring exist; engine implementation is NOT required for the field to count as implemented. A knob with an entry and no accessor is a light switch with no wire behind it | — | A field counts as implemented once its storage, its default and its wiring into the central hub exist. Whether anything downstream consumes it is a different question, and not part of this one. |                                                      |
| 9.135 | Does not relax wiring honesty — a key counts as engine-wired only when its value reaches a decision. Hub-complete and engine-side debt coexist: both facts tracked, neither hides the other | — | This does not relax the separate honesty rule about downstream consumption: something can be fully wired at that central point and still owe work further along. Both facts are tracked, and neither is allowed to hide the other. |                                                      |
| 9.136 | Never scalarise a reference vector type because it "obviously" holds one value: that is a judgement call on someone else's data model, and those lose — the schema describes THEIR format, not our reader, and scalarising eats data on round trip | — | Never narrow a reference format's collection type to a single value because it obviously holds only one. That is a judgement call about someone else's data model, and those lose. The schema describes their format, not your reader, and narrowing it silently drops data on a round trip. |                                                      |
| 9.137 | Cannot read it as a vector yet → a vector-aware accessor, or a recorded gap; never a schema that lies to flatter the reader | — | If you cannot read the collection form yet, write an accessor that could, or record the gap. Never write a schema that lies in order to flatter your reader. |                                                      |
| 9.138 | Accessors keep the KEY's name — the accessor for a singular key returns a collection, it does not get renamed to a plural; plurality lives in the type, not in a private vocabulary | — | An accessor keeps the name the source format uses, even when it returns a collection. Plurality lives in the type, not in a private vocabulary you invented. |                                                      |
| 9.139 | Swapping one scalar representation for another can be right — a numeric field becoming a string to carry a form the numeric type cannot hold — and is not a plurality call | — | Swapping one single-value representation for another can be entirely right, when the new one carries a form the old type could not. That is a different question, not a decision about plurality. |                                                      |
| 9.140 | Layout is validated against the RENDERED result, never assumed from markup: every interactive element fully visible, unobstructed and within reach at all supported viewport sizes — nothing clipped, occluded or outside the visible area | — | Validate a layout against what actually renders, never against the markup that produced it: every interactive element fully visible, unobstructed and reachable at every supported window size, with nothing clipped by a container, hidden behind an overlay, or off the visible area. |                                                      |
| 9.141 | Spatial output — interface, diagrams, print layout — is reasoned about as final rendered geometry; plausible-looking source is not a usable result | — | Where the output is spatial, reason about the final rendered geometry. Source that looks plausible does not mean a result anyone can use. |                                                      |
| 9.142 | Position encodes relationship: a control sits adjacent to its target, in the same visual group, and moves with it; its scope is inferable from location alone, and it is never separated from its target by unrelated content or independent layout flow | — | Position encodes relationship: a control sits next to the thing it acts on, inside the same visual group, and moves with it. Its scope should be inferable from where it is alone, and unrelated content never comes between them. |                                                      |
| 9.143 | The cant-break-by-design audit's DENOMINATOR is generated by a script from the source, so it is reproducible rather than asserted: it emits every entry the audit must dispose of | — | The denominator of an audit is generated from the material itself, so it is reproducible rather than asserted. It lists every entry the audit is obliged to dispose of. |                                                      |
| 9.144 | Part A, STATED claims: a doc comment on a public field, function or type whose text claims an invariant — never, must, only, always, by construction, sole, guaranteed, cannot, stamped. The audit asks: is there a MECHANISM, or only the sentence? | — | The first half of the denominator is every stated claim: a comment on something public whose words assert an invariant. Of each the audit asks one question — is there a mechanism, or only the sentence? |                                                      |
| 9.145 | Part B, OBLIGATION shapes: code shapes carrying an invariant obligation whether or not anyone stated one. The audit asks: is it stated, and is it enforced? | — | The second half is the obligation shapes: patterns in the code that carry an invariant obligation whether or not anybody ever stated one. Of each the audit asks two questions — is it stated, and is it enforced? |                                                      |
| 9.146 | Shape S1, coupled fields: a field's doc names a sibling field | — | Obligation shape: a field whose documentation names a sibling field, describing a coupling that nothing enforces. |                                                      |
| 9.147 | Shape S2, derived data: a value stored twice — a count beside a collection, or a value beside a suffixed variant of itself | — | Obligation shape: the same fact stored twice — a count kept beside the collection it counts, or a value kept beside a variant of itself. |                                                      |
| 9.148 | Shape S3, variant payload: a struct holding an enum field plus fields meaningful for only some variants | — | Obligation shape: a record holding a kind alongside fields that are meaningful for only some of those kinds. |                                                      |
| 9.149 | Shape S4, sentinel: a comparison against a magic value, or clamping a domain value at zero | — | Obligation shape: a comparison against a magic value, or a domain value clamped at zero — both signs that a particular number is carrying a second meaning. |                                                      |
| 9.150 | Shape S5, unproven non-empty: taking the first or last element with a call that fails on an empty collection | — | Obligation shape: taking the first or last element of a collection with a call that fails when it is empty, where nothing proves it is not. |                                                      |
| 9.151 | Shape S6, bare primitive: a quantity field typed as a raw number where a dedicated type already exists | — | Obligation shape: a quantity carried as a raw number in a place where a dedicated type for it already exists. |                                                      |
| 9.152 | Shape S7, swappable args: a public function with two or more adjacent same-typed primitive parameters | — | Obligation shape: a public function with two or more adjacent parameters of the same simple type, which a caller can transpose without any complaint. |                                                      |
| 9.153 | Shape S8, lifecycle order: public begin, init, set, finalize, end or reset methods taking a mutable receiver | — | Obligation shape: public methods that begin, initialise, set, finalise, end or reset on something mutable — a required order that nothing enforces. |                                                      |
| 9.154 | Shape S9, discarded result: a call whose result is thrown away or defaulted away | — | Obligation shape: a call whose result is thrown away or quietly replaced by a default, so a failure has nowhere to be seen. |                                                      |
| 9.155 | Inline unit tests are excluded — they are not the shipped surface — but dead or test-only code found OUTSIDE those blocks still gets an entry and must be dispositioned, never silently dropped | — | Test code inside the shipped units is excluded as not being the shipped surface; but dead or test-only code found outside it still gets an entry and is dispositioned like any other. Nothing is silently dropped from the denominator. |                                                      |
| 9.156 | The generator is deterministic: an unsorted set scan made the evidence differ run to run, once even naming different siblings, which breaks regenerate-and-diff — the verification the artifact rests on | — | The generator is deterministic: anything unordered is sorted before it reaches the output. Otherwise the same input produces different evidence on different runs, which destroys regenerate-and-compare — the verification the whole artifact rests on. |                                                      |
| 9.157 | Evidence text has all whitespace collapsed at the writer, because a multi-line match would split one entry into two rows and make the file's line count disagree with the entry count | — | The generator sanitises its own evidence text at the point of writing, so one entry can never break into two rows and make the file's line count disagree with the entry count. |                                                      |
| 9.158 | Structural check on the audit table: every finding row carries exactly the declared number of columns, every id is unique, and every id exists in the denominator; the check prints rows, denominator and remaining | — | The audit table is checked structurally: every row has the declared number of columns, every identifier is unique, and every identifier exists in the denominator. The check prints the row count, the denominator and how many entries remain. |                                                      |
| 9.159 | Every actionable row lands in EXACTLY ONE fix, asserted mechanically, so a fix cannot be quietly dropped from the work list while remaining open in the ledger | — | Every actionable finding lands in exactly one fix, and that is asserted mechanically. A fix then cannot be quietly dropped from the work list while its findings stay open in the ledger. |                                                      |
| 9.160 | Two artifacts, two jobs: the audit table is the LEDGER, dispositioning all entries and proving nothing was skipped; the fix list is the WORK LIST, deduplicated by fix so one remedy appears once instead of once per row | — | Two artifacts with two jobs: the table is the ledger, dispositioning every entry and proving nothing was skipped, and the fix list is the work list, deduplicated so one remedy appears once instead of once per finding. |                                                      |
| 9.161 | Ordering the fix list by CONSEQUENCE is a judgement call, stated as such in the report; everything else — row memberships and counts — is derived | — | The one judgement call in the report — the order of the fixes, by consequence — is labelled as a judgement call. Everything else, the memberships and the counts, is derived mechanically. |                                                      |
| 9.162 | Rows found already enforced and rows retired as not-an-invariant are reported as counts and excluded from the work list, never simply deleted | — | Findings already enforced, and findings retired as not really invariants, are reported as counts and left out of the work list. They are dispositioned, not deleted. |                                                      |
| 9.163 | Fix 11 is explicitly BLOCKED: the variant-payload shape cannot yet tell a real obligation from a false positive — it missed the one case proven real — so a usage-based detector must run before any of those 43 rows is called a fix or a false positive | — | A shape whose detector cannot yet tell a real obligation from a false positive is declared blocked, with the detector work named as the prerequisite. Its entries are not called fixes and not called false positives until that runs. |                                                      |
| 9.164 | The report states what the method does NOT cover: invariants nobody wrote down that match no shape, and that row counts are not effort — one fix is mechanical retyping across hundreds of rows, another is 47 sites that change what the user sees today | — | The report states what the method cannot see: invariants nobody ever wrote down that match none of the shapes. It also says row counts are not effort, because a large mechanical fix and a small user-visible one look the same in a count. |                                                      |
| 9.165 | — | Every release ships a migration rewriting ALL persisted layouts on the card to the current version, unconditionally — the floor case being a migration whose only effect is to raise the stamp. Enforced as arithmetic against a committed baseline; the amendment striking "if migration is needed" is what made it checkable | ✗ not in union: Every release ships a migration that rewrites all persisted state on the real target device to the current version, unconditionally — the floor case being a migration whose only effect is to raise the version stamp. It is enforced as arithmetic against a committed baseline, because a conditional form leaves a judgement call no check can catch. |                                                      |
| 9.166 | — | When the UI detects a migration it discards local storage and re-seeds from the card: at a version boundary the card wins outright, and the ordinary reconciliation governs only the non-migration case | ✗ not in union: When the client detects that a migration has happened, it discards its local copy and re-seeds from the device. At a version boundary the device's copy wins outright; the ordinary reconciliation governs only the case where nothing migrated. |                                                      |
| 9.167 | — | During a migration the served entry document is replaced by an "Upgrade in process" page, so no browser loads the app mid-migration; because migrations are unconditional, this window is entered on EVERY release | ✗ not in union: While a migration runs, the served entry page is replaced by an upgrade notice, so nothing loads the product half-migrated. Because migrations are unconditional, that window is entered on every release. |                                                      |
| 9.168 | — | One stamp IS the layout version: it owns layout migration and is what the ratchet reads; the other stamps the non-layout configuration document and carries no claim about what a layout means — which requires layouts to LEAVE that document rather than be re-labelled | ✗ not in union: One version stamp owns the persisted arrangement and is the one the ratchet reads. The other stamps the rest of the configuration and makes no claim about what an arrangement means — which requires the arrangement to leave that document rather than merely be re-labelled inside it. |                                                      |
| 9.169 | — | A canvas-format change FORCING a configuration-document change is coupled so neither stamp can advance alone — by a mechanism, not a prose "must", since the two stamps have deliberately different disciplines: one ratchets every release, the other is read by equality and bumps only on a real shape change | ✗ not in union: Where a change to one format forces a change to the other document, the two are coupled so neither version stamp can advance alone — by a mechanism, not a prose "must", because the two stamps deliberately have different disciplines: one advances every release, the other is compared for equality and moves only on a real change of shape. |                                                      |
| 9.170 | — | The configuration document records the operator's EDITS, not the app's resolved STATE: geometry reaches the overlay only from an operator gesture, so presence in the file IS authorship and an item at its coded default is absent — which is what lets a later release move or re-measure it | ✗ not in union: The stored configuration records the operator's edits, not the product's resolved state: a value reaches it only from an operator's gesture. Presence in the file is therefore authorship, and anything still at its built-in default is simply absent — which is exactly what lets a later release move or re-measure it. |                                                      |
| 9.171 | — | Attribution is STRUCTURAL, not stored: write-through on gesture beats a persisted provenance mark, because a mark states authorship a second time beside the file's own key presence and nothing makes the two agree. Prospective only: already-resolved values are repaired by a ruled migration or an explicit reset, never by inferring which a person chose | ✗ not in union: Authorship is structural, not stored: writing the value through at the moment of the gesture beats recording a separate mark saying who set it, because such a mark states authorship a second time beside the file's own contents and nothing keeps the two in agreement. The rule is prospective only — values already written by the old design are repaired by a ruled migration or an explicit reset, never by guessing which of them a person chose. |                                                      |
| 9.172 | All code carries full instrumentation and flame-chart tracing capability; NEW code ships with it | — | All code carries full instrumentation and profiling capability, and new code ships with it rather than acquiring it later. |                                                      |
| 9.173 | Old code gains it ORGANICALLY: whenever a change touches a site that lacks it, the change adds it — no active retrofit scans | — | Existing code gains instrumentation organically: whenever a change touches a site that lacks it, that change adds it. There are no retrofit sweeps, because the work rides along with changes that were happening anyway. |                                                      |
| 9.174 | Tracing: the measure attribute on every non-trivial function a change adds or touches, so the perf ledger and flame charts see it without a second pass | — | Every non-trivial function a change adds or touches gets the profiling annotation in that same change, so the performance record sees it without a second pass over the code. |                                                      |
| 9.175 | Diagnostics: every new decision site gets a default-inert `FS_*` trap or a stage-trace row — denominator stated in the output, explicit zeros, probe shape matched to the question, a positive control | — | Every new decision site gets a diagnostic switch or trace row that is inert until turned on, and it states its denominator in the output, prints explicit zeros rather than omitting them, matches its shape to the question being asked, and ships a positive control. |                                                      |
| 9.176 | A new trap registers in the traps index in the SAME change | — | A new diagnostic switch is registered in the diagnostics index in the same change that introduces it, so the list of what can be turned on is never behind the code. |                                                      |
| 9.177 | The bar: when this site misbehaves a year from now, can it be interrogated WITHOUT editing code? If not, the change is not done | — | The bar is one question: when this site misbehaves a year from now, can it be interrogated without editing code? If not, the change is not done. |                                                      |
| 9.178 | A diagnostic's LEVEL is decided once, per code, in the code's own `level()` — emission sites never pass a level ("why are these warnings then and not info level?") | — | How serious a diagnostic is gets decided once, for each diagnostic code, in one place. The sites that raise it never pass a level of their own, so the same condition cannot come out as a warning in one caller and a passing remark in another. |                                                      |
| 9.179 | A code is declared ONCE, in one list carrying its documentation, its wire name and its level together; the enumeration, the wire format and the level table are generated from that entry, never hand-maintained beside it. The hand-written array was the wire test's denominator: a stale entry failed to compile, a MISSING one compiled and silently narrowed the guard to 23 of 24 codes | — | Each diagnostic code is declared once, in a single entry that carries its documentation, its external name and its level together, and everything else — the list of codes, the external format, the level table — is generated from that entry. A list kept by hand beside it is the denominator of the test that guards the format, and a missing line there compiles cleanly while quietly narrowing the guard. The external name stays a written-out literal rather than being derived from the code's own name, so renaming a code fails loudly instead of silently rewriting every message already in the field. |                                                      |
| 9.180 | A computed result's warnings are an INSEPARABLE part of its return value, never an optional out-parameter — an out-parameter lets a caller pass a throwaway and drop the warning silently | — | Whatever a computation warns about comes back as part of its result, never as a separate optional output. A separate one lets a caller hand it somewhere disposable and drop the warning without anybody noticing. |                                                      |
| 9.181 | Commodity plumbing reuses an existing generator's PUBLIC ENTRY POINT rather than forking or reimplementing it | — | → cant-break-by-design § 4. The process — the tripwire; reuse the one route rather than forking it |                                                      |
| 9.182 | A crate that declares `hotpath` must instrument something, or appear on a declared gap list that can only shrink; the instrumentation rule is unenforceable where the tool is absent, and absence looks exactly like compliance | — | A unit that declares itself profiled either instruments something or is named on a declared gap list that may only shrink. Where the profiling tool is simply not present the instrumentation rule cannot be enforced at all, and that absence looks exactly like compliance. |                                                      |
| 9.183 | A default-inert profiler can never supply a figure the program prints; a user-facing cost keeps its own wall-clock, measured once at the site that states it, never a second clock inside the measured span | — | A profiler that is off by default can never be the source of a number the program itself prints. A cost shown to a user keeps its own clock, read once at the site that states it, and never a second clock started inside the span being measured. |                                                      |
| 9.184 | A measurement of one run is a value that run produces, never an accumulator that outlives it; the numerators and the denominator they are read against travel in the SAME value | — | A measurement of one run is a value that run produces, never a running total that outlives it. The counts and the total they are read against travel together inside that one value, so a second run in the same process cannot report the first run's work over its own count. |                                                      |
| 9.185 | Where the registry has already scalarised a key by ownership and the hub routes it to its owning target, the plurality lives in the ROUTING and the plural-correct read is the scalar one | — | Judge whether a value is one or many against the data model actually in front of the reader, not the name of the type upstream. Where the store has already reduced a key to one value per owner and routes it to the owner it belongs to, the plurality lives in that routing, and reading it as a single value is the plural-correct read; reading it as a collection returns nothing on every load and lets a fallback win instead. |                                                      |

## 10. Reference Sources

*Both projects arrived at this independently and in almost the same words, so
most rows here carry two filled columns. One project's position is the settled
end of a real wobble: an early rule required every module to cite the source it
ported and later named a single tree as the only legitimate reference. Both
halves were overturned — the citations moved out of the code, and the other
trees were restored as legitimate sources — while the no-verbatim-porting half
was never in question. Only the standing rules are rowed. Where reading a
reference produces a claim about the product's own output, the comparison
discipline is 3.29–3.34 and 12.31–12.43.*

Owner ruling, 2026-09-01: rows 10.6–10.14 are marked `✗ not in union`; phase 3
does not carry them; 10.1–10.5 remain.

| id | ferrislicer | dwc-ng | proposed universal form | |
|---|---|---|---|---|
| 10.1 | Reference trees are read for semantics and behavioural contracts only | ALL vendored and example third-party source, any installed dependency opened to study, and any code seen on a board, in a repo or on the web is REFERENCE ONLY: it exists to show HOW a problem was solved and WHAT edge cases exist | Everything you read from someone else's implementation — vendored source, an installed dependency you opened to study, code seen on a device, in a repository or on the web — is reference only. It is there to show how a problem was solved and which edge cases exist. | |
| 10.2 | No verbatim porting from any tree: the source is a behavioural contract, the design is owned, and a novel implementation is preferred | You may NEVER copy, port, transcribe, translate or line-by-line paraphrase it: take the understanding, then write it from scratch to fit our own architecture | Never copy, port, transcribe, translate or paraphrase line by line. Take the understanding, then write the implementation from scratch to fit your own architecture. The reference is a behavioural contract; the design is yours. | |
| 10.3 | C++ file and line citations belong in specs and research docs, never in this project's code | Citing reference by file and line to explain a decision is ENCOURAGED; lifting its code is forbidden | Citing a reference by file and line to explain a decision is encouraged; lifting its code is forbidden. Those citations live in specifications and research documents, never in the product's own source. | |
| 10.4 | — | A standing project rule, not licence-driven caution to be waived: it holds regardless of the vendor's licence | This is a standing rule about how the work is done, not caution about someone's licence. It holds no matter what the licence permits. | |
| 10.5 | — | If a solution seems to require copying, STOP and ask | If a solution seems to require copying, stop and ask rather than deciding for yourself that this case is the exception. | |
| 10.6 | Four trees under one root are ALL legitimate; cite the tree a fact ACTUALLY came from and lead with the one that owns it | Names its reference set explicitly: everything under `reference/` (DWC, `@duet3d/*`, the M409 docs) plus any installed dep opened to study | ✗ not in union: More than one reference implementation may be legitimate at once. Cite the one a fact actually came from, and lead with the one that owns it. | |
| 10.7 | One tree is FIRST PASS: the primary reference, the correctness target and the base of the config dialect; a parity question is answered against it | — | ✗ not in union: One reference is the first pass: the primary source, the correctness target, and the base of the vocabulary. A question about matching behaviour is answered against that one. | |
| 10.8 | A second tree is a COMPATIBILITY TARGET, not first pass: not banned, never erased — some fields exist only there, and its profiles are meant to migrate; cite it where the thing is only its | — | ✗ not in union: A second reference may be a compatibility target rather than a first pass: not banned and never erased from lineage, because some things exist only there and its data is meant to migrate in. Cite it where the thing is only its. | |
| 10.9 | A fork of one of them is a reference source in its own right, cited DIRECTLY where it is the actual source, because it measurably DIVERGES from what it forked | — | ✗ not in union: A fork of a reference is a reference in its own right and is cited directly where it is the actual source, because it diverges from what it forked and the difference is measurable. | |
| 10.10 | The ancestry the others descend from is named as such | — | ✗ not in union: Where one reference is the ancestor the others descend from, name it as that rather than treating it as an equal peer. | |
| 10.11 | A citation naming the WRONG tree is a defect even when the symbol exists in several of them: it sends the next reader to a file that does not contain the value | — | ✗ not in union: A citation that names the wrong source is a defect even when the same symbol exists in several of them, because it sends the next reader to a file that does not contain the value. | |
| 10.12 | Treat the original paper or canonical library as the algorithm's source of truth, and the shipping implementations only as behaviour references, never as the oracle | — | ✗ not in union: Where a published specification or canonical implementation exists, that is the source of truth for the algorithm. The shipping implementations are behaviour references, not the oracle. | |
| 10.13 | When reference implementations disagree, CLASSIFY the disagreement — algorithm principle, production consensus, implementation-specific behaviour, known bug pattern, or profile-tuned behaviour — before deciding which to follow | — | ✗ not in union: When two references disagree, classify the disagreement before choosing: a difference of principle, a consensus of practice, something specific to one implementation, a known defect, or tuning. Choosing without classifying is copying the one you read last. | |
| 10.14 | Persistence formats are this project's own: no cross-product interoperability format is adopted just because a reference has one | — | ✗ not in union: The product's own persisted formats are its own. Adopting a reference's interoperability format merely because it has one is a design decision in disguise, and it is not made by default. | |

## 11. Environment & Platform

*The stale-build-cache rule sits with the working-copy discipline that causes
it, at 5.16, and is not re-rowed here; the failing-loudly half of tool
resolution is the same demand as 8.31, stated at a different trigger. What is
here is the project's decision about which platform layers exist at all, how a
tool is resolved, and what may enter the dependency set.*

| id | ferrislicer | dwc-ng | proposed universal form | |
|---|---|---|---|---|
| 11.1 | "No WSL in this project until otherwise stated" — literal: a scope decision Gabe can lift, not a claim that WSL is unusable | — | A whole platform layer can be ruled out of a project by decision. State it literally: it is a scope decision the owner can lift, not a claim that the excluded thing does not work. | |
| 11.2 | The POSIX shell is Git Bash at a named absolute path; Windows-native tooling plus that shell are the WHOLE toolchain, and nothing invokes the excluded launcher | — | Name what the toolchain actually is — the shell, at its full path, plus the platform-native tools — and let nothing in the project invoke the excluded layer. | |
| 11.3 | Bare `bash` is NOT the shell you meant: PATH lookup can land on the operating system's launcher stub for the excluded layer, which prints its refusal in an unexpected encoding and exits non-zero, so a clean file was reported as broken | — | A bare command name is not the tool you meant. Resolving it by search path can land on a same-named stub the operating system ships, which fails in a way that reads as your own file being broken rather than the wrong tool having run. | |
| 11.4 | Resolve a real shell by explicit path and reject any candidate under the known system directories where the stub lives | — | Resolve the real tool by explicit path, and reject candidates in the known locations where the lookalike lives. Rejecting the known-bad ones is part of resolving, not a separate check. | |
| 11.5 | A tool that cannot find a usable one FAILS LOUDLY, naming what it looked for — never a quiet skip, because a skipped check reads as a pass | — | A tool that cannot resolve what it needs fails loudly, naming what it looked for. It never skips quietly, because a skipped check reads as a pass. | |
| 11.6 | Adding any new external crate dependency requires STOPPING and getting explicit user authorization first | NEVER add a dependency without asking Gabe first | Never add a new external dependency without stopping and getting the owner's explicit authorization first. | |
| 11.7 | — | Prefer zero-dep or low-dep packages | Prefer dependencies that bring few or no dependencies of their own. | |
| 11.8 | — | Frozen lockfile installs only | Installs run against the frozen lock file only, so an install can never quietly move a pinned version. | |
| 11.9 | — | Lifecycle scripts blocked by default; allowlist exactly one package in `onlyBuiltDependencies` | Package lifecycle scripts are blocked by default, with an explicit allowlist naming each package permitted to run one. | |
| 11.10 | — | `minimumReleaseAge: 4320` in the workspace config | A published version must be a stated minimum age before it may be installed, so a freshly published compromised release is not picked up on the day it lands. | |
| 11.11 | — | pnpm 10+ | The tool that installs dependencies is itself pinned to a stated major version, because the policy switches above depend on features that version provides. | |

## 12. Standing Agents

*One project's alone: the other has no standing agent definitions, so its
column is empty throughout. Rows 12.1–12.10 are the conventions every agent
definition shares; 12.46 is a further cross-cutting one, found late, and 12.47
a second, added by the coverage sweep. The rest
are one agent at a time — job and trigger first,
then inputs, then the output contract. Several agent behaviours restate a rule
whose trigger lives in an earlier group; those rows say so and cite the row
rather than repeating it, and the note "trigger belongs with" marks a demand
that would have been rowed earlier had the agent file been read then. Where an
agent's model tier is fixed by its definition, the economy rules at 4.5 and
4.23 are what fixed it.*

| id | ferrislicer | dwc-ng | proposed universal form | |
|---|---|---|---|---|
| 12.1 | Each agent is one file declaring name, the question it answers, the typical triggers that should reach for it, the exact tools it gets, and the model it runs on | — | Each standing agent is declared in one file: the question it answers, the triggers that should reach for it, the exact tools it is given, and the model it runs on. Nothing about it is folk knowledge. | |
| 12.2 | The auditing agent answers ONE question and nothing else; bugs, style, naming and performance are out of scope unless they ARE the mechanism failure — say so and move on | — | A standing agent answers one question and says which. Anything outside it is named in a line and dropped, not pursued, because an agent that widens its own scope stops being predictable. | |
| 12.3 | Containment is STRUCTURAL, not advisory: the auditing agent has no command, edit or write tool, so it cannot check out, reset, stage or move a branch — the capability was REMOVED rather than forbidden, after a review subagent once force-moved the main branch | — | Containment is structural, not advisory: an agent that only judges is given no ability to run commands or write files, so it cannot move or damage anything. The capability is removed, not forbidden — a rule in the prompt is a promise, and this followed a reviewing agent force-moving the shared branch. | |
| 12.4 | The consequence is stated in the agent's own definition: it cannot run a diff, so the CALLER must supply the diff text or the changed-file list; given neither, it says exactly that and stops — never auditing the whole tree as a substitute, never guessing from timestamps | — | Removing a capability has a consequence, and the agent's own definition states it: it cannot work out its own scope, so the caller supplies it. Given neither, it says exactly that and stops — it never substitutes the whole codebase, and never guesses the scope from timestamps or file contents. | |
| 12.5 | Where a job genuinely needs Bash — building and running a comparison — containment is weaker, so it is enforced in prose the agent must hold to: an allowlist of build, test, script and read-only inspection commands | — | Where a job genuinely needs to build and run things, containment is weaker and the definition says so, replacing it with an explicit allowlist of the build, test and read-only inspection commands the agent may use. | |
| 12.6 | FORBIDDEN for such an agent: any command that moves a ref or mutates the working tree — checkout, switch, reset, rebase, merge, commit, stash, push, force-branch, worktree add or remove. "You verify; you do not integrate" | — | An agent that verifies never integrates: every command that moves a reference or mutates the working copy is forbidden by name, not left to judgement. | |
| 12.7 | A reviewer works READ-ONLY: never creates, moves or deletes branches, never merges, force-resets shared refs or pushes | — | An agent that judges does not touch: no reviewer creates, moves or deletes branches, merges, force-resets a shared reference, or pushes. | |
| 12.8 | A reviewer never stages protected tracking files — the owner's audit list, feature list, agent instructions, or the planning directory | — | A reviewing agent never stages the owner's own protected working records, whatever else it was asked to look at. Trigger belongs with group 5, where the same files are protected at commit time (5.11). | |
| 12.9 | Each parallel porting agent owns EXACTLY ONE crate and writes only files under that crate's directory, never editing outside it | — | An agent doing implementation work in parallel with others owns exactly one unit and writes only files inside it. Ownership is by directory, so two agents cannot collide over the same file. | |
| 12.10 | An automated generation model NEVER commits directly to a protected branch: an adversarial review pass and a human reviewer both stand between it and the shared branch | — | An agent that generates content never commits to a protected branch. An adversarial review pass and a person both stand between it and the shared line. | |
| 12.11 | Audit output is a SUSPICION, never a fact: a scanner-flagged candidate may only be acted on after a human reads the code and records a file-and-line confirmation — never because "the tool said so" | — | A scan's output is a suspicion, never a fact. A flagged candidate is acted on only after a person has read the code and recorded a confirmation naming the exact place — never because the tool said so. | |
| 12.12 | Invariant auditor — job: which stated invariant does this change weaken, and at which rung is it ACTUALLY enforced? Triggers: a change touching code that states an invariant, a pre-merge audit of a campaign branch, a new choke-point, a review of code whose doc claims "by construction" | — | The enforcement auditor answers one question: which stated invariant does this change weaken, and how strongly is it really enforced? It runs when a change touches code that states an invariant, before a branch merges, when a new single route is introduced, and over any code whose comment claims construction. | |
| 12.13 | It reads the enforcement ledger FIRST, loading the sections for the touched crates; the ledger registers only CLAIMED invariants, so a true-but-unstated property is not a row and must not be invented | — | It reads the enforcement ledger first, loading the parts covering what changed. The ledger holds only claimed invariants, so a property that is true but was never stated is not a row, and the auditor must not invent one. | |
| 12.14 | For each changed region it finds the CLAIM by grepping the file and its module for the vocabulary of assertion — by construction, invariant, must never, guaranteed, always, sole, only route, caller must, precondition, assumes | — | For each changed region it finds the claim by searching that file and its surroundings for the vocabulary of assertion, because the claim is whatever the source promises a reader. | |
| 12.15 | It rates the real mechanism, not the sentence — constructor, field visibility, module boundary — and runs the tripwire and the grandfathering obligation as explicit steps | — | It rates the mechanism rather than the sentence, by reading the constructor, the visibility and the unit boundary, and it runs the second-call-site tripwire and the obligation to strengthen or record weak neighbours as explicit steps of the procedure. Those rules themselves are 9.10, 9.8 and 9.78. | |
| 12.16 | The anti-pattern catalog must ALWAYS be caught, and each flag cites the analogous live instance in the ledger | — | The standing catalog of ways a claim outruns its mechanism (9.49–9.75) is always flagged, and each flag cites a comparable live example, so the finding is a pattern with evidence rather than an opinion. | |
| 12.17 | Output per finding: the CLAIM quoted in the source's own words with path and line; the STATED rung the prose implies; the ACTUAL rung the mechanism delivers with the specific reason; and the PROMOTION that reaches rung 7–8, or the exact ledger row to add instead | — | Each finding has a fixed shape: the claim quoted in the source's own words with its location, the strength the prose implies, the strength the mechanism actually delivers and the specific reason for the gap, and either the concrete change that would reach the top of the scale or the exact debt row to add instead. | |
| 12.18 | It closes with a LEDGER DELTA: rows this change makes stale, and rows it should add | — | It closes with the delta to the ledger: which entries this change makes stale, and which ones it should add. | |
| 12.19 | Report only what is substantiated by a file actually read, ordered by severity; a clean audit is a useful result stated in ONE line and never padded | — | It reports only what it can substantiate from a file it actually read, ordered by severity, and a clean result is stated in one line rather than padded into a report. | |
| 12.20 | Never claim a mechanism you did not read: if the constructor is in a crate you did not open, say the rating is UNVERIFIED rather than inferring it from the type's name | — | It never claims a mechanism it did not read. Where the deciding code was not opened, the rating is reported as unverified rather than inferred from a name. | |
| 12.21 | Config-field agent — job: one config field end to end across registry entry, type object, hub wiring, generation, inheritance and provenance, cross-product import, and gates. Triggers: add a key, "why is this value not reaching the engine", audit a field's provenance, pre-merge review of a change to the registry or the hub | — | The field agent owns one configuration field end to end, across every surface it touches: its definition, its type, its wiring, whatever is generated from it, its inheritance and origin, its arrival from foreign inputs, and the gates over all of that. It runs to add one, to audit one, to explain why a value is not arriving, and to review a change touching those surfaces. | |
| 12.22 | It does NOT own the design of any layer: it points at the authority for each and verifies against it, reading it fresh every time — restating a fact that lives elsewhere is how that fact rots | — | It owns none of those layers' designs. For each it names the authority and verifies against it, read fresh every time, because restating elsewhere a fact that lives somewhere is how that fact rots. | |
| 12.23 | Locate the field's current state across all seven surfaces BEFORE proposing any change: a field can be complete at one layer and absent at another — name which | — | Locate the current state on every surface before proposing any change, and name which layers it is complete at and which it is missing from. Something can be finished at one layer and absent at the next. | |
| 12.24 | Make the change at its ONE authority — the hand-authored source of truth, the wiring module, or the design record — never at a generated mirror | — | Make the change at its one authority — the hand-authored source, the wiring, or the design record — and never at anything generated from them. | |
| 12.25 | Regenerate, never hand-patch, a generated file; if you cannot run its generator, SAY SO AND STOP rather than editing the output by hand | — | Regenerate a generated file; never hand-patch one. If the generator cannot be run, say so and stop rather than editing its output. | |
| 12.26 | Run the gates that touch what you changed and report their REAL output — pass or fail plus the printed denominator; a gate that reports OK with no denominator is not evidence | — | Run the gates covering what was changed and report their real output, never a paraphrase: the verdict and the count it printed. A gate reporting success with no count is not evidence. The count requirement itself is 8.21. | |
| 12.27 | Ship a test with the change — unit test, fixture generator, integration coverage — with no exemption for "just a config key" | — | The agent ships a test with its change, with no exemption for work that feels too small to need one. Trigger belongs with group 3, where the standing test obligation is 3.21. | |
| 12.28 | Gabe's standing rule: an undecided field is TICKETED, never blocked on and never silently resolved — file the pair and stop rather than picking an answer | — | An undecided question is filed as work and the agent stops. It is never blocked on, never silently resolved, and never improvised: an agent applies authority, it does not adjudicate design. Trigger belongs with group 6, whose ticket-pair machinery is 6.15–6.18. | |
| 12.29 | Stop condition: a value with NO clear provenance state under the standing design — such as one arriving from a foreign import, currently unratified | — | Stop and file when a value has no defined origin under the standing design — for instance one arriving from another product's file, where nothing has been decided yet. | |
| 12.30 | Stop condition: a PRECEDENCE question the standing design does not resolve, such as a new override axis it never contemplated | — | Stop and file when two sources could both supply a value and the standing design does not say which wins. | |
| 12.31 | Stop condition: a key hub-complete but with NO clear downstream consumer, where it is not obvious which of the three legitimate not-yet-wired categories it belongs to | — | Stop and file when something is complete at the central point where settings are wired in but has no clear downstream consumer and no obvious reason from the recognised list for why it has none. | |
| 12.32 | Stop condition: any change needing an 8th variant in the design's own state set — that is an amendment to the design record, Gabe's call, not a field-by-field judgment | — | Stop and file when the change would need a new case in the design's own state set. That is an amendment to the design, the owner's call, not a judgement to make one field at a time. | |
| 12.33 | Stop condition: a generated file whose generator cannot be run — missing external checkout, missing tool — and no safe hand-edit exists | — | Stop and file when a generated file needs to change, its generator cannot be run, and there is no safe hand-edit. | |
| 12.34 | Scope boundary: it is not the invariant auditor or the parity verifier — hand off to those for their questions and do not duplicate their checks | — | Each agent names the other agents whose questions are not its own, hands those off, and does not duplicate their checks. | |
| 12.35 | Parity verifier — job: run the golden-oracle comparison and deliver a verdict a merge decision can rest on. Triggers: before merging any change that can move output — engine or geometry edits, schema default changes, algorithmic or performance rewrites | — | The comparison agent runs the product against the known-good reference and delivers a verdict a merge decision can rest on. It runs before merging anything that could move the product's output: engine changes, default changes, and algorithmic or performance rewrites. | |
| 12.36 | Its one non-obvious job: the EXIT CODE is not the verdict — the harness exits non-zero on any mismatch and one mismatch is known pre-existing; a false alarm on that has cost real time, and a "pass" because a different row failed while the old one was fixed is worse | — | Its one non-obvious job: the exit status is not the verdict. The harness fails on any mismatch and one mismatch is known and expected, so reporting failure on that is a false alarm — and reporting a pass because a different row failed while the old one was fixed is worse. The rule itself is 3.29. | |
| 12.37 | Establish the BASELINE before judging: diff against the caller's baseline set, or the recorded pre-existing one; if a mismatch cannot be confidently attributed, label the run UNBASELINED and say plainly you cannot separate regression from pre-existing — never resolve the ambiguity by guessing | — | Establish the baseline before judging: compare against the set the caller supplied, or the recorded pre-existing one. If a difference cannot be confidently attributed, label the run unbaselined and say plainly that you cannot separate a regression from something pre-existing. The ambiguity is never resolved by guessing. | |
| 12.38 | Classify EVERY non-informational row as a new mismatch, a pre-existing one, or newly FIXED — regressions are the finding, but an unexplained fix usually means a tolerance or a measurement changed, not the engine | — | Classify every row that is not informational as newly broken, already broken, or newly fixed. Regressions are the finding, but report the fixes too: an unexplained improvement usually means a tolerance or a measurement moved, not the product. | |
| 12.39 | Informational rows never affect the exit code and never affect the verdict | — | Rows marked informational never affect the verdict, and the definition says so, so nobody has to decide case by case. | |
| 12.40 | NEVER pipe the harness through head, tail or any truncation: the row table IS the evidence, and a truncated table has hidden failures here before; if the output is long, read all of it | — | Never truncate the comparison's output. The table is the evidence, and a truncated one has hidden failures before. If it is long, read all of it. The general form is 3.23. | |
| 12.41 | A schema DEFAULT change must run this harness: getter-level fallbacks drift silently, and a default change that only passes the unit suite is unverified | — | A change to a default value must run the comparison. Fallbacks further down drift silently, and a default change that only passes the unit suite is unverified, not verified. | |
| 12.42 | Tolerances are deliberate and per row class — a wide one exists to catch an order-of-magnitude fault, not a styling difference — and are never tightened to manufacture a finding | — | Tolerances are deliberate and stated per kind of row, with a wide one existing to catch a fault of the wrong order of magnitude rather than a cosmetic difference. They are never tightened to manufacture a finding (3.34). | |
| 12.43 | Read the harness's own hardcoded reference-binary path rather than assuming it; artifacts land in a known directory; the compared side is the RELEASE build, so build that first | — | Read the harness's own configuration — where it expects the reference to live, where it writes its artifacts, which build of the product it compares — rather than assuming any of it. | |
| 12.44 | Output: lead with a ONE-LINE verdict — PASS, REGRESSION, UNBASELINED or PARTIAL — then the full row table verbatim, new mismatches with values and deltas and the likely subsystem, pre-existing ones named and dismissed with the reason, skipped cases with what would enable them, and the environment actually used | — | Lead with a one-word verdict, then the full table word for word, the new differences with both values and the likely area responsible, the expected ones named and dismissed with the reason, the cases that did not run and what would enable them, and the environment actually used. | |
| 12.45 | A missing prerequisite — reference absent at the hardcoded path, base file unsaved, release binary will not build — is reported as a BLOCKED run naming the missing prerequisite; never substitute a partial run for a verdict | — | A missing prerequisite is reported as a blocked run that names what was missing. A partial run is never substituted for a verdict. | |
| 12.46 | If the cited authority's `Status` line is not `Accepted`, its content is still the best available description of shipping behaviour — but treat any of its own open questions as genuinely open | — | Where the authority for a decision is a record that has not been ratified, its content is still the best available description of how the thing actually behaves. But any question that record itself leaves open stays genuinely open: reading it does not close it. | |
| 12.47 | A verification gate is MANDATORY on generated proposals: one that does not survive the automated checks — build, tests, the relevant metrics, parity — is discarded WITHOUT being read | — | Automatically generated proposals pass a verification gate before any person spends attention on one: whatever does not survive the build, the tests and the relevant checks is discarded unread. Attention is the scarce thing the gate protects. | |

## 13. Register System

*Group 2 covers how a rule gets FILED — the capture hook, the intake procedure,
what a filing owes. This group is the register itself: what the document is, how
it is structured, and what its checker mechanically verifies. Both projects run
the same checker, one having ported it from the other, so most rows here carry
two columns. Demands already rowed at their own trigger are not repeated: citing
by section rather than by line is 2.12, stamping a supersession both ways is
2.16, an undispositioned inbox entry failing the check is 2.5, the cheap-versus-
full split is 2.23, a blank-line citation blocking everywhere is 2.34, saying
where the rows came from is 2.33, and stating each group's real enforcement is
2.21. Rows 13.24–13.32 widen the group one step, from the register alone to the
family of documents it belongs to — the dated ruling that is meant to go stale,
the living map that must not, and the two filing judgements that keep one rule
from becoming two; 13.27–13.32 were added by the coverage sweep.*

| id | ferrislicer | dwc-ng | proposed universal form | |
|---|---|---|---|---|
| 13.1 | Two documents, two jobs: the register is the living authority on WHAT was decided, the enforcement ledger on HOW STRONGLY code holds each invariant; they cross-link, they never duplicate | Same split, same sentence; here the ledger sibling is generated from declarations beside the mechanisms | Two documents, two jobs: one records what was decided, the other records how strongly each decision is actually held in code. They cross-link and they never duplicate, so every fact has exactly one of them as its home. | |
| 13.2 | Everything else that states a rule — specs, plans, refactor docs, the learnings notebook — is IMMUTABLE HISTORY that the register cites | Identical wording, same kinds of document | Everything else that states a rule — specifications, plans, the learnings notebook — is immutable history that the register cites. It is never edited to reflect a later decision, because history that gets rewritten stops being evidence of anything. | |
| 13.3 | Rules bearing on the same design decision — agreeing, refining or disagreeing — share a group, sorted by adoption date | Identical contract | Rules that bear on the same decision live in one group, whether they agree with it, sharpen it or contradict it, ordered by when each was adopted, so the group reads as the history of one argument rather than a list. | |
| 13.4 | Group status is one of three circles: settled (agreeing rules, or a wobble with a clear written verdict), weak agreement (the verdict is only inferred, or a documented deviation was never reconciled), and unsettled (opposing rules both still live) | Identical three states, same definitions | Every group carries one of three status marks, with fixed meanings: settled — the rules agree, or the disagreement ended in a written verdict; weakly settled — the verdict is only inferred, or a deviation is documented but never reconciled; unsettled — opposing rules are both still live. | |
| 13.5 | A verdict marked *(inferred)* was read from the latest-dated rule and current docs, never separately adjudicated; the marker is dropped and the ruling cited when a real adjudication lands | — | A verdict nobody actually ruled on is labelled as inferred, so a reader can tell a reading from a decision. The label comes off, and the ruling is cited, the moment a real decision lands. | |
| 13.6 | A rule with no sibling goes to a Solo area, grouped by area, with no common-thread summary — and Solo headers carry NO status circle | Its contract provides for the same Solo section, created when the first solo rule arrives | A rule with no sibling is filed in a by-area section that carries neither a summary nor a status mark, because a lone rule has no disagreement to characterise and nothing to declare settled. | |
| 13.7 | A Contents list heads the file: every group by name, with its status circle and its rule count | Same Contents list, status circle per entry | The register opens with a contents list naming every group, its status and how many rules it holds. That list is the cheap surface a search for where-does-this-belong reads first (2.13). | |
| 13.8 | Every row is three columns: the adoption date, the rule as ONE sentence, and the citation to its durable home | Identical row shape | Every row carries three things and only three: when the rule was adopted, the rule in one sentence, and a citation to where the rule actually lives. | |
| 13.9 | The maintenance contract is a section INSIDE the register, and it names the checker that enforces it | Identical section, same name, same position | The rules for maintaining the register are a section of the register itself, and they name the check that enforces them. | |
| 13.10 | The checker's header enumerates its BLOCKING checks and, separately, its advisory output, which never affects the exit code | Identical header and split, with the ported list adapted | A checking tool's own header enumerates what it blocks on and, separately, what it only advises. Advisory output never changes the exit status, so a reader of either the header or the output can tell the two apart. | |
| 13.11 | Check: every row citation names a file that EXISTS — line drift is tolerated, existence is the gate | Same check, kept unchanged in the port | Every citation is checked to name a file that exists. Line numbers drifting is normal and tolerated; the file being gone is not. | |
| 13.12 | NAMING NOTE in the file's own header: the citation checks are STRUCTURAL only — the file exists, the line is not blank — and never that the cited line supports the claim beside it; a drifted-but-non-blank citation passes silently, by design | — | A check whose name sounds like it validates something states in its own header exactly what it validates and what it does not. These check that a citation names a file that exists and a line that is not empty, never that the line still supports the claim beside it — a citation whose target drifted onto different real content passes, deliberately, and the header says so. | |
| 13.13 | Check: every intent-group header carries exactly ONE status circle; Solo headers carry none | Same check, forward-compatible with a Solo section this register does not have yet | The status marks are checked mechanically: exactly one on every group heading, none on a lone-rule heading. | |
| 13.14 | Check: stamps are bidirectional — every retirement stamp under the docs tree names a register group that EXISTS, and every `Supersedes:` line names a real file carrying a stamp that points back | Identical check; the stamp text was already byte-for-byte what this project's own contract specified, so nothing needed adjusting | Retirement is checked in both directions: every stamp on a retired document names a group that exists, and every supersession row names a file that exists and carries the stamp pointing back. One direction alone is a dangling link that neither side can see from where it stands. | |
| 13.15 | Check: every section citation resolves to a real heading, matched by PREFIX, so a row may cite "Agent cost economy" against a heading that continues "(HARD RULE)" | Identical check and identical prefix rule | Every citation naming a section resolves to a real heading in that file, matched on its leading words so a row may name a heading while the heading itself carries a suffix. | |
| 13.16 | Headings are read with fenced code blocks SKIPPED: a `#`-prefixed line inside a quoted snippet matches the heading pattern exactly, and a citation could have "resolved" to it | Same guard, ported with the reason restated | When a check reads a document's headings, it skips fenced code blocks. A line inside a quoted snippet can match the heading pattern exactly, and a citation would then resolve to something that is not a section at all. | |
| 13.17 | A file cited ONLY in section form never matches the line-citation scan, so the section check reports its own missing files rather than assuming the other check covers it | Same reasoning, restated in the ported comment | Where two checks split a space between them, each covers its own half completely. A file cited only in the form one check reads is reported missing by that check, never left to the other one, which will never see it. | |
| 13.18 | The checker reads only the rule TABLES, never the prose around them, so the contract that DOCUMENTS the citation format does not fail the rule it states — same shape as the inbox check requiring column zero, so the header's own example does not read as a live entry | Identical, with both examples kept | A check over a document reads only the rows that carry live claims, never the prose around them. Otherwise the section documenting the format fails the rule it is stating, and the example in a header reads as a real entry. | |
| 13.19 | An address shape belonging to another gate is SKIPPED rather than half-checked, and the limitation is stated outright: mistype a numbered heading and nothing here catches it; fixing that needs a per-document convention, which is not pretended to exist | The port made that same branch FAIL instead of skip, because the gate it deferred to does not exist here — otherwise a mistyped citation of that shape would pass silently forever | A check hands a case off only to another check that actually exists, and says in its own text what it handed off and what that leaves unverified. Where no such second check exists, the case fails rather than being skipped, because a skip with nobody behind it passes forever. | |
| 13.20 | — | A check that could NEVER fail in this project is dropped, not ported: the ledger it resolves against has no numeric headings here, so porting it verbatim would produce a check that always passes. The omission, and what covers its ground instead, are stated in the file's own header | A check that cannot fail in this project is dropped rather than carried across, and its absence is stated in the tool's own header along with what covers that ground instead. A check that can never fail proves nothing and reads exactly like one that works. | |
| 13.21 | Advisory: an unsettled group with no ticket reference anywhere near it | Identical advisory, kept in the port | An unsettled group that names no ticket draws an advisory. An open disagreement with nowhere to follow it is how one gets forgotten. | |
| 13.22 | Advisory, on request: rule-marker lines found in files no register row cites — one note per file, because the point is that the file is uncited | Identical drift scan, pointed at this project's own rule-bearing paths | On request, the check scans named files for rule-shaped lines and notes any file no row cites — one note per file, because the finding is that the file is uncited, not how many such lines it holds. | |
| 13.23 | The checker's own failure modes are red-checked against fixtures by a self-test that runs beside it, in the commit gate and in the hosted check | The port was falsified live instead: three real violations introduced (a citation to a missing file, a second status circle, a pending inbox entry), what each mode caught recorded, then reverted and both modes confirmed green again | Prove the checker can fail before trusting it: either a self-test that red-checks every blocking failure mode against fixtures, or a live falsification that introduces one real violation per check, records exactly what each mode caught, and reverts. A gate nobody has seen fail is indistinguishable from a broken one. | |
| 13.24 | A dated ruling record is not a policy manual: once accepted its body is FROZEN — only a status transition or a trivial, decision-preserving correction may edit it | — | A dated decision record is not a policy manual. Once it is accepted its body is frozen: the only permitted edits are a change of status and a correction that preserves the decision exactly. | |
| 13.25 | A decision change lands as a NEW, superseding record that also flips the old record's status line — never as an in-place edit | — | A changed decision lands as a new record that supersedes the old one and flips the old one's status in the same change. It never lands as an edit to the old record. | |
| 13.26 | The living/dated distinction is the point of the group: the map and the register are LIVING documents kept current with the code; a dated ruling is supposed to go stale | — | Know which kind of document you are editing. Some are living and are kept current with the code; a dated ruling is meant to go stale, and updating one to match today destroys the record rather than maintaining it. | |
| 13.27 | The other living document is the map: every bug fix and implementation updates the pipeline mapping in the SAME change, and the alignment data and page too when config keys or gaps moved | — | The document that maps how the system behaves is updated in the same change as the code, along with any data or page generated beside it. It is not a follow-up task, because a map that lags is worse than no map: it is read and believed. | |
| 13.28 | "Update" is defined, not left to taste: the affected stage's section reflects the new behaviour, a fixed divergence is re-marked with the commit that fixed it, and a landed gap ticket flips its status on the page | — | What counts as updating the map is defined rather than left to taste: the section for the part you changed describes the new behaviour, a difference you closed is re-marked with the change that closed it, and an item whose work landed has its status flipped. | |
| 13.29 | A fix in a stage not yet mapped at logic depth maps that stage FIRST, to the map's own template — never guess | — | Fixing something in an area the map does not yet describe at the depth you need means mapping that area first, to the map's own template. Working from a guess about an unmapped area is how the map and the code start disagreeing. | |
| 13.30 | A stale map is a FAILING TEST, not missing docs | — | A stale map is a failing test, not a documentation gap. It is treated with the urgency of something broken, because that is what it is. | |
| 13.31 | — | A rule that matters to two groups is filed in the ONE whose trigger it fires on and cross-referenced from the other with the reason, deliberately not duplicated as a second row | A rule that matters to two groups is filed in the one whose trigger it actually fires on, and cross-referenced from the other with the reason for the split. It is never copied in as a second row, because two copies drift and neither knows it. | |
| 13.32 | — | Two rules that look alike stay APART when the remedy differs: the near-identical group's rule is discharged by running any check at all, while here a check WAS run and returned a true answer to the wrong question — so the fix is a different check, which that rule would never have produced | Before folding a new rule into a group that looks like its home, ask whether that group's own remedy would have produced this one's fix. If it would not, they are different rules however similar they read, and folding them loses the one that was not already there. | |

## 14. Commit Gates

*What runs at commit time, and on what terms. The individual gates are rowed
where their own trigger lives and only cross-referenced here: the advisory sweep
warning is 5.14, the generated-document check and its self-test are 6.22–6.23,
the output filter's own suite running blocking at commit time is 8.29, the
register check's contents are group 13, and the advisory register nudge is 2.8.
One project runs a five-check blocking hook plus a partly-overlapping hosted run
— it repeats one of the hook's five checks, in its fuller form, and adds four
the hook never runs, while leaving four of the hook's five with no hosted
backstop at all (the merge gate is where those are caught) — while
the other runs a one-check hook and has no hosted checks at all, which is where
14.3's conflict comes from.*

| id | ferrislicer | dwc-ng | proposed universal form |                                                                                           |
|---|---|---|---|-------------------------------------------------------------------------------------------|
| 14.1 | The hook's header enumerates every BLOCKING check with the ticket that added it, and separately every ADVISORY one | The hook's header states its scope, its fast/full split and its bypass, in the script itself | A commit gate's own header names every check it runs, says for each whether it blocks or only advises, and states how the gate is activated and how it is bypassed. The script is where a reader looks first, so that is where the description lives. |                                                                                           |
| 14.2 | Mechanical checks BLOCK; content matching over documents may WARN, never reject — the standing no-language-matching ruling | — | A mechanical check may block. A check that matches on the content of prose may only warn, never reject: guessing at what a sentence means is not something to stake a commit on. |                                                                                           |
| 14.3 | Every blocking check runs unconditionally on every commit; the cheap ones are deliberately not gated on the staged file list | Only commits touching one of five governance paths are gated at all: "a register violation cannot be introduced by a commit that does not touch the register, its citation targets, or the inbox — and a hook that runs on every commit regardless is a hook people disable" | ⚔ A cheap commit check runs on every commit, not only on commits that touch what it checks: the file-list condition is itself a judgement about where a violation can come from, and it is silent when that judgement is wrong. The overruled form gates only the commits that touch the checked surface, arguing that a gate firing on every commit is one people switch off. Same ruling as 2.6, stated there for the register check specifically. dropped nuance: the narrow form also keeps every unrelated commit fast, which is what made the check affordable to its author. | i suspect we have different kinds of checks that have bled together making it take longer |
| 14.4 | A citation is validated ONCE, at authoring time, over the lines the diff ADDS; existing citations are never re-audited and later drift is accepted, not reported — a continuous re-check of all ~410 was rejected as noise that gets disabled | — | Validate a citation once, when it is created, over the lines the change actually adds. Never re-audit the ones already there: their line numbers moving is normal, and a check that goes red on ordinary drift is noise somebody eventually turns off. |                                                                                           |
| 14.5 | The commit hook is the natural home for a creation-time check — creation time is exactly the moment this hook runs | — | A creation-time check belongs in the commit gate, because the moment a commit is made is the moment the thing is created. |                                                                                           |
| 14.6 | Content is read from the INDEX, or in merge mode from the tip being merged, NEVER the working tree, so a partially-staged file cannot make the gate pass or fail on the wrong bytes | — | A commit gate reads content from what is actually being committed, never from the working copy. Otherwise a half-staged file makes the gate pass or fail on bytes nobody is committing. |                                                                                           |
| 14.7 | Deliberately NOT checked, and said so in the header: whether the cited line SUPPORTS the claim beside it. No heuristic was found that would not risk flagging a correct citation, so it is left undone rather than approximated | — | The check states in its own header what it deliberately does not check, and why. Judging whether a citation supports the claim beside it needs a person, and a heuristic that flags correct ones would get the whole check disabled — so it is left undone and said so (9.96). |                                                                                           |
| 14.8 | The gate and its self-test are excluded from their own scan: the self-test's fixtures are citation-SHAPED data, not real claims — found by dogfooding the gate against its own commit | — | A checking tool excludes itself and its own tests from its scan, because a self-test's fixtures are check-shaped data rather than real claims. Run the tool against its own change on purpose; that is how this gets found. |                                                                                           |
| 14.9 | Every run prints "validated N new citation(s)", N=0 included, because most commits add none and that must read as a normal pass rather than silence indistinguishable from a broken diff | — | Every run prints its own count, including zero. Most runs have nothing to check, and that has to read as a normal pass rather than as silence that looks exactly like a broken check (8.21). |                                                                                           |
| 14.10 | A blocking check on the enforcement ledger's TABLES: every row carries the header's cell count, because a short row still renders — it just silently drops or merges a column, in the one file whose whole purpose is that a claim resolves to its mechanism | — | A table people rely on is checked structurally rather than proofread: a row with the wrong number of cells still renders, it just renders wrong, silently dropping or merging a column. Where the document exists so a claim can be traced to its mechanism, an eaten cell is exactly the failure it exists to prevent (9.158). |                                                                                           |
| 14.11 | The same check fails on an unresolved `TICKET-PENDING` placeholder: a row that names no ticket says so with an em dash, never with an IOU | — | The same check fails on a placeholder standing in for a reference nobody filled in. A row that names nothing says so plainly, rather than leaving an IOU that reads like a pending fact. |                                                                                           |
| 14.12 | A gate written and never wired is not enforcement: a dead-gate audit found a table checker written months earlier and never called, and a live output filter with no tests at all — both are now blocking at commit time, where the consequence lands, and cost was not a factor (~0.007s) | — | A check that exists but nothing runs is not enforcement. Audit for those deliberately, and wire each one where the consequence of its regression actually lands. Runtime is rarely the reason not to. |                                                                                           |
| 14.13 | Hooks live in a tracked directory in the tree, activated once per clone by pointing the hooks path at it — never self-installing | Same shape, with the reason in the hook itself: a hook nobody can see in the tree is a hook nobody maintains, and one that installs itself silently is worse | Commit hooks are tracked in the repository and activated per clone by one explicit command. A hook nobody can see in the tree is a hook nobody maintains, and one that installs itself silently is worse. |                                                                                           |
| 14.14 | — | The bypass is stated in the hook itself, for a genuine emergency: "if you find yourself doing that twice, the checker is wrong — fix the checker" | The gate names its own escape hatch for a genuine emergency, and names what using it twice means: that is evidence the checker is wrong, and the fix is the checker, not the habit. |                                                                                           |
| 14.15 | The hosted document-governance run is SEPARATE from the build workflow and needs no build toolchain: a full engine build per document edit would teach people to batch document changes, which is how registers rot | — | The hosted checks over documents are kept apart from the ones that build the product, and need none of the build toolchain. A full build for every documentation edit teaches people to batch documentation edits, which is exactly how a register rots. |                                                                                           |
| 14.16 | It runs on the platform where every one of its gates was MEASURED passing — an unmeasured platform gate is noise | — | A hosted check runs on the platform where its gates were actually measured passing. A gate measured somewhere else is noise wherever it ends up running. |                                                                                           |
| 14.17 | Every gate in it was measured passing on the commit that introduced it, and each has a demonstrated red-check — each checker's own self-test runs as a gate beside it | — | Every hosted gate was measured passing on the commit that introduced it, and each has a demonstrated way of going red: the checkers run their own self-tests as gates beside them, because a gate nobody has seen fail is indistinguishable from a broken one. |                                                                                           |
| 14.18 | Its trigger paths enumerate exactly the files its gates read, plus a manual dispatch | — | The hosted check's triggers name exactly the files its gates read, and it can also be started by hand. |                                                                                           |

## 15. Merge Gates & Ratchets

*The merge gate is one project's; the other has no merge-time gate and no hosted
checks, so most rows here carry one column. Its individual legs are rowed where
their own trigger lives and cross-referenced from here: echoing a sub-check's
denominator is 8.21, the heartbeat is 8.23–8.27, judging a comparison by its
mismatch set is 3.29, never rebaking a baseline to make a check pass is
3.30–3.31, the poisoned shared build cache is 5.16, and the merge bar itself is
5.15. What is here is the gate as a system — what it runs against, how it
judges, what it refuses to read as a pass — and how a ratchet over a large
standing baseline behaves.*

| id | ferrislicer | dwc-ng | proposed universal form | |
|---|---|---|---|---|
| 15.1 | RUN IT ON THE MERGE RESULT, NOT ON THE BRANCH — the whole point. One change compiled perfectly on its own branch and broke `main` on merge (a method the other side had deleted); another's docs merged cleanly into a format outlawed thirty minutes earlier | — | Run the gate on the merged result, never on the branch alone. A branch that passes tells you nothing about the tree you are about to push: the other side may have deleted what you call, or outlawed the form you wrote, since you branched. | |
| 15.2 | The gates moved local when the hosted service stopped executing: 100 of the last 100 runs failed in ~2 seconds with no runner assigned, so five gates enforced nothing for two days while nine merges landed | — | When the hosted checks are absent, or turn out not to be running, the gates move somewhere that actually executes — locally, at merge time. Confirm they run rather than assuming it: a service can fail in a way that reports failure for days while everything keeps landing. | |
| 15.3 | Judged by DIFFING the failing set against a committed baseline, never by exit code: the workspace suite exits non-zero on a clean tree because of a standing red set, so an exit-code gate would mean either ignoring it or never passing it | — | Where a tree has a known standing set of failures, judge a run by diffing its failure set against a committed baseline, never by its exit status. An exit-status gate over a tree that is never fully green means either ignoring the gate or never passing it (3.29). | |
| 15.4 | Newly-GREEN tests are reported too, loudly: a test that starts passing is either good news or a test that stopped testing | — | Report the newly passing cases as loudly as the newly failing ones. A case that starts passing is either good news or a check that has stopped checking, and nothing in the result itself tells you which. | |
| 15.5 | The gate builds into its OWN build directory by default; concurrent builds from several working copies into one directory poison the cache and produce errors that contradict the source — measured: the same tree failed in the shared directory and passed in an isolated one. Sharing stays available behind an explicit override | — | The gate builds in its own directory rather than sharing one with the working copies. A shared build directory under concurrent use produces errors that contradict the source (5.16), and the gate is the worst place to be diagnosing that. Sharing stays available as an explicit, deliberate override. | |
| 15.6 | The quick mode skips the full sweep for iterating and is NEVER used for a merge; what it skipped prints as skipped, never as passed | — | A quick mode that skips the expensive sweep exists for iterating and is never used for a merge. What it skipped prints as skipped, never as passed. | |
| 15.7 | Each gate captures the COMMAND's own exit status, never a pipe's: a trailing `tail` or `grep` reports the LAST command's status, which has silently turned a red battery green more than once here | — | Every check captures the exit status of the command being checked, never of something piped after it. A trailing filter reports its own status, and that has turned a red run green more than once. | |
| 15.8 | Prerequisites are resolved ONCE, up front, before any gate runs; an unresolvable interpreter aborts the whole script loudly, naming what was looked for, rather than letting a downstream call fail quietly and leave a stale artifact a later gate could misread as a real verdict | — | Resolve everything the run depends on once, before the first check. What cannot be resolved aborts the whole run loudly, naming what was looked for — never a quiet failure later, which can leave a stale artifact that a subsequent check reads as a real verdict. | |
| 15.9 | A gate whose command can exit 0 having matched ZERO cases must additionally require evidence that at least one ran: a name filter that stops matching prints "0 passed; 0 failed" and exits 0, identical to a healthy run at the exit-code level | — | Where a check can succeed having run nothing — a name filter that stops matching, a scan that walks no files — it must additionally require evidence that at least one case actually ran. Nothing-of-nothing and a healthy pass are the same exit status. | |
| 15.10 | Absence of a verdict is a HARD FAILURE, never a fall-through to an unset or stale bucket; an unrecognised verdict category is a hard failure too | — | A missing verdict is a hard failure, never a fall-through to whatever the last run left behind. An unrecognised verdict is a hard failure too: only the outcomes the gate defines are allowed to pass. | |
| 15.11 | ONE component owns the parsing, the evidence check and the baseline file; the gate only RENDERS its verdict. Two private greps over the same output were two opinions of one fact and would have disagreed eventually | — | One component owns parsing a result, checking its evidence and holding its baseline; the gate only renders that verdict in its own vocabulary. Two independent readings of one output are two opinions of one fact, and they will eventually disagree (9.89). | |
| 15.12 | The changed-file sub-gate captures the diff command's own exit status: swallowing it would turn a real failure into an empty file list, which reads identically to "no files changed" — a crash wearing a pass | — | A check that works from a list of changed files verifies that the command producing the list actually succeeded. Swallowing its failure yields an empty list, which reads exactly like "nothing changed": a crash wearing a pass. | |
| 15.13 | Format only the files a change touches, with the pinned stable formatter, invoked per file | — | Format only the files a change actually touches, with the pinned formatter, invoked one file at a time. | |
| 15.14 | Never at a crate root: handing the formatter a crate root reformats sibling files the change never touched — whole-tree sweeps hit ~50 unrelated files, and every agent that tried one had to revert | — | Never hand the formatter a package or directory root. It reformats sibling files the change never touched, and that has to be reverted every time. | |
| 15.15 | Two named crates are exempt from formatting entirely, to preserve their pre-existing drift; new files there may be formatted individually, and generated directories are skipped | — | Named parts of the tree can be exempt from formatting altogether, to preserve drift that predates the rule, and generated output is skipped. New files inside an exempt area may still be formatted one at a time. | |
| 15.16 | The build must be WARNING-FREE: an unused private or crate-internal function is deleted or explicitly test-gated, never left to generate a silent unused-code warning | — | The build is warning-free. Something unused is deleted, or explicitly marked as existing for the tests, never left generating a warning nobody reads — a warning that is always present is one nobody notices the day it means something. | |
| 15.17 | Two compile gates, not one: everything with ALL features and all targets, and separately the feature-OFF build, because an all-features build cannot see a crate that BREAKS with a feature off | — | Compile twice, not once: once with every optional feature on and every target included, and once with the optional features off. A build with everything switched on cannot see the code that breaks when something is switched off. | |
| 15.18 | Lint runs the correctness and suspicious classes only — a deliberate scope, not the whole lint set | — | The lint leg runs a deliberately chosen class of lints rather than everything available, so the gate is about defects rather than taste. | |
| 15.19 | Complying with the echo-your-denominator rule was done AT THE CALL SITE — re-reading the log this one gate had just written — rather than changing the shared helper's behaviour for sixteen gates inside an unrelated ticket | — | Comply with a new rule at the call site that owes it, rather than changing shared behaviour for every caller inside a change that was about something else. The wider change is a decision of its own and gets its own work. | |
| 15.20 | The scanner's OWN tests run inside the gate before the ratchet reads its numbers: the scanner is workspace-excluded, so nothing else ever runs them — and a check nothing runs is not enforcement, which is a defect this very tool detects | — | A tool the ordinary suite never reaches has its own tests run by the gate that consumes it, before its numbers are trusted. A check nothing runs is not enforcement. | |
| 15.21 | The scanner is a REPORTER that always exits 0 over a large audited standing baseline, so the gate over it is a RATCHET, not a pass/fail over absolute counts — which would be red on arrival, and a gate red on arrival is forbidden | — | Where a measurement produces a large, already-audited standing count, the gate over it is a ratchet rather than a pass-or-fail on the absolute number. A gate that is red the day it lands is one everybody learns to ignore. | |
| 15.22 | Any detector count ABOVE the baseline fails: that is new debt | — | Any count above the recorded baseline fails the gate. That is new debt, and stopping it arriving quietly is what the gate is for. | |
| 15.23 | A count that DROPPED is reported so the baseline is lowered in the same change that earned it | — | A count that has dropped is reported, so the baseline can be lowered in the same change that earned the drop. A ratchet nobody tightens stops being one. | |
| 15.24 | A detector not present in the baseline FAILS: it is added deliberately, with the regeneration flag, in a reviewed commit — never automatically | — | A category the baseline has never seen fails rather than being adopted silently. It is added deliberately, in a reviewed change. | |
| 15.25 | A baseline detector that VANISHED is reported for update rather than silently ignored | — | A category that has disappeared from the measurement is reported so the baseline gets updated. It may mean the work is done or that the detector stopped detecting, and silence hides both. | |
| 15.26 | Regeneration is a deliberate, reviewed commit behind an explicit flag; recording the current failing set as accepted is used ONLY when the change to the red set has been decided correct | — | Regenerating a baseline is deliberate, explicit and reviewed. Recording today's failures as the accepted set happens only when somebody has decided that change to the failing set is correct (3.30). | |
| 15.27 | Scope: SHIPPING code only — the scan skips the non-shipping siblings of the source directory and prints per-category SKIPPED counts, because an exclusion nobody can see is indistinguishable from a walk that missed the files | — | The ratchet measures shipping code only, and prints how much it skipped, per category. An exclusion nobody can see is indistinguishable from a walk that missed those files: both print a clean result. | |
| 15.28 | When the scope was corrected, the baseline was rewritten off the CORRECTED scan in the same commit — never off the old numbers, which would have forgiven real debt along with the miscounted | — | When a measurement's scope is corrected, the baseline is rewritten from the corrected measurement, in the same change. Keeping the old numbers would forgive real debt along with the part that was miscounted. | |
| 15.29 | Before that fix the ratchet counted integration tests as production debt — 842 of 1298 findings in one crate's tests, against ONE in its source — penalising compliance with the standing test mandate | — | A gate that punishes compliance with another standing rule is miscalibrated, not strict. When a count turns out to be dominated by work some other rule required, the scope is what is wrong. | |
| 15.30 | A detector returning ZERO findings is broken until proven otherwise, and so is a scan that parsed no files — both FAIL rather than pass | — | A detector that finds nothing is broken until proven otherwise, and so is a scan that read no files. Both fail rather than pass, because both look exactly like a clean result (9.104). | |
| 15.31 | A merge gate never trusts merge-parent order alone: diff against BOTH parents, or resolve the pre-merge tip directly — covered by a wrong-direction merge fixture that asserts the gate goes RED | — | A gate that works out what a merge changed never trusts the order of the merge's parents alone. Compare against both, or resolve the pre-merge tip directly, and pin it with a case built the wrong way round that proves the gate goes red. | |
| 15.32 | The toolchain version is pinned EXPLICITLY in the verification configuration — not a floating channel, not read from a file the build can override — and bumping it is a deliberate, visible one-line commit | — | The toolchain the verification runs on is pinned explicitly, in the verification's own configuration: not a floating channel and not read from somewhere the build could override. Moving it is then a deliberate one-line change somebody can see. | |
| 15.33 | A known flake is fixed at its ROOT CAUSE: never papered over with a retry, a rerun-failed action, an ignore marker, or forcing single-threaded runs | — | A check that fails intermittently is fixed at its cause. Never paper over it with a retry, a rerun-the-failures step, an ignore marker, or forcing everything to run one at a time — each of those keeps the gate green while it stops meaning anything. | |
| 15.34 | A feature switch that limits what SHIPS must not also limit what gets COMPILED during verification: keep the switch for dependency weight, but build all features when verifying | — | A switch that limits what ships must not also limit what gets compiled while verifying. Keep it for what it is for — weight in the shipped product — and build everything when checking, or the code behind it is never compiled at all. | |
| 15.35 | A regression census records MAGNITUDE, not just presence or count, or a defect can double in place while the gate stays green | — | A count of known problems records how big each one is, not merely that it exists. Recording presence alone lets a defect double in place while the gate stays green. | |
| 15.36 | A gate parses the DOCUMENT and the CODE and fails if they disagree: the decision record states the provenance type's variant set explicitly, so an eighth variant either updates that record in the same change or breaks the gate. Run both in the hosted check and in the merge gate | — | A document that states a fact about the code is machine-checked against the code at the gate, so the fact cannot drift quietly: the change that moves the fact either updates the document or turns the gate red. Where the document could instead be generated from the code, that is the stronger remedy and this check is the fallback for where it cannot (9.63). | |
| 15.37 | Two models of the same pipeline, plus the preview page's embedded copy, must agree — checked in structure mode where the full checker cannot run. It exists because the forward and backward models once drifted three commits apart unnoticed | — | Two artifacts that must describe the same thing are checked against each other at the gate and advance together. Unchecked they drift quietly, and you end up with two descriptions of one thing, several changes apart, with nothing saying which is current. Where a check cannot run in full, run the part of it that can rather than skipping it. A generator that derives one artifact from the other is stronger still (9.63). | |
